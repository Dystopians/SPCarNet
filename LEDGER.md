# GEMS LEDGER (Stage One COMPLETE → Stage Two ACTIVE)

## STAGE TWO ENTRY (2026-07-02)
- **Human approval of Stage-1→Stage-2 transition RECORDED**: user directive "按照你认为最好的方式来,在你认为完整完成了stage1时进行下面的任务 docs/GEMS_Stage2_Prompt.md" — explicit instruction to proceed to Stage Two upon Stage-One completion, given with full knowledge of the honest Stage-One state (all E-tests FAIL; compaction core validated; report + recommendation delivered). Stage One completed 2026-07-02 (commit 05adc6c).
- Entry conditions verified: STAGE1_REPORT ✓ (candidate-completion + this approval); fresh-env repro PASS (Δ0.0005 dB, same day); `audit_test_path` on final artifact: first run RED — scope artifact (train-side `teacher_factory.py` inside the eval-audited `tools/gems/`; dynamic eval-path check was clean) → relocated to `tools/gems_train/` → **AUDIT GREEN** (`eval/stage2_entry_audit2`).
- Stage-Two state files: `CLAIMS.md` (C1 full; C2 demoted-to-measurement; C3 sans teacher; C4 bounded-to-preservation + NON-CLAIMS), `MATRIX.md` (cells+status). Stage-Two goals continue the same GOAL numbering.

### GOAL #009 — Stage-2 bootstrap + first Tier-1 batch [Matrix: E1-PARETO S-REND] — 2026-07-02 — IN PROGRESS
- CLAIMS.md + MATRIX.md written; scene registry expanded to all 9 M360 scenes (frozen ingestion configs: outdoor images_4 / indoor images_2, llff8; ONE FT config all scenes per D7 — features 0.0016, positions/weights 0; NOTE: indoor originals trained with update_indoor lrs, our FT config is deliberately uniform).
- PREDICTION (serves C1/C3): B5 (evidence prune + features-FT) at B50 lands within −0.10 dB of clean on ≥7/8 remaining S-REND scenes (garden precedent: above clean); B4 (no-FT) within −0.20; any scene failing → bounds C1 and goes to E9-FAIL.
- RUNNING: 8 scenes × {B5, B4} @B50, tag s2, 4 detached chains (GPUs 1/2/3/5), logs `gems_stage1/logs/s2_gpu*.log`. Remaining for E1-PARETO S-REND: B25/B12.5 budgets, B2/B3 baselines, aggregate analysis.
- SS3DM acquisition (D-1) = next blocking cell after this batch.

Persistent memory for the GEMS Stage One loop (`docs/GEMS_Stage1_Prompt.md`, v1.0, 2026-07-02).
At every /goal: read this file top-to-bottom, then PROTOCOL.md (once it exists), then act.
Never trust chat memory over this file.

---

## FROZEN DECISIONS (change only with explicit justification logged here)

| ID | Decision | Rationale | Date |
|----|----------|-----------|------|
| DEC-001 | GEMS durable output root = `/data/peilincai/gems_stage1/` **only if** /data freed above 50 GB; otherwise `/home/peilincai/gems_stage1/` (root volume, 5.9T free). Currently: **`/home/peilincai/gems_stage1/`** | `/data` is 100% full (42 GB free) — below the D6 50 GB floor. `/dev/shm` also full (never use per D6). | 2026-07-02 |
| DEC-002 | `dev_real_A` = Mip-NeRF360 **garden**, dev resolution = `images_4`, `--resolution -1` (matches existing clean checkpoint config) | Clean 30k checkpoint + reproduction metrics already exist at this config (PSNR 24.71 / SSIM 0.762 / LPIPS 0.216, iter 30000). Avoids retraining; fine-tunes are affordable. | 2026-07-02 |
| DEC-003 | SS3DM town data is **NOT on disk** (`/data2/peilincai/SS3DM_raw` gone). `dev_drive_A` must use a fallback; candidates: ETH3D courtyard (GT laser scan? verify), T&T barn/truck (COLMAP present; GT?), `parking_phone_tiny_anonymized` (domain-perfect, no GT mesh). Final pick in M0. | Per prompt §2 "facts M0 must verify" — verified absent. | 2026-07-02 |
| DEC-004 | Python env = micromamba `mesh_splatting` (`/home/peilincai/micromamba/envs/mesh_splatting`, py3.10). GPUs: prefer 3, then 1/2/5 (0/6/7 busy with other users' jobs). | Environment survey. | 2026-07-02 |
| DEC-005 | Clean baseline = existing `outputs/carnet/meshsplatopt/paper_m360_repro/official_clean30k/<scene>/` checkpoints (iteration_30000; 26000 also retained). Do NOT retrain full9. | 9/9 scenes trained, metrics recorded, match paper. Retraining wastes days and /data has no space. | 2026-07-02 |
| DEC-006 | `dev_drive_A` = **ETH3D courtyard** (`/data/peilincai/mesh_datasets/eth3d_colmap/courtyard`, 38 imgs, COLMAP + points3D.ply; GT laser scans at `mesh_datasets/eth3d/courtyard/courtyard/{scan_clean,dslr_scan_eval}/scan{1,2}.ply`). T&T barn/truck have NO GT on disk; parking_phone_tiny has no GT mesh (keep as optional qualitative extra). Caveat: llff every-8 split → ~5 test views; PROTOCOL must set split honestly (candidate: every-4). | Only available scene with GT geometry → enables g1/g4. SS3DM absent (DEC-003). | 2026-07-02 |
| DEC-008 | **Storage RESOLVED by human decision (2026-07-02)**: user approved deleting `outputs/carnet/meshsplatopt/ecsr_phase_{s,f,r}` (~1.1 TB, decommissioned v1xx–v3xx axis). Deletion manifest: `/home/peilincai/gems_stage1/deletion_manifest_20260702.txt`. After deletion, GEMS durable output root moves to **`/data/peilincai/gems_stage1/`** (updates DEC-001); preflight still mandatory before every run. `official_clean30k` untouched. | User selected "Delete ecsr_phase_s/f/r" via AskUserQuestion. | 2026-07-02 |
| DEC-007 | **STORAGE CRISIS (superseded by DEC-008).** Verified: `/` volume has a hard 100 GiB **user quota**, currently ~1 GiB headroom (torch.save died mid-write; dd probe stopped at 941 MiB). `/data` = 41 GB free, no user quota, below the 50 GB D6 floor. No other writable volume. Interim interpretation of D6 (documented, not a waiver): KB–MB artifacts (code, docs, metrics.json, panels) may be written; **GB-scale artifacts (checkpoints, render sets, teacher caches, toy dataset training) are BLOCKED** until human chooses: (a) approve retention-policy cleanup of decommissioned v1xx–v3xx dirs in `outputs/` (2.2 TB, reclaim ≥300 GB), (b) raise home quota, or (c) explicitly waive the 50 GB floor for /data with a tight per-run budget. | D6 + honesty; deleting user research artifacts autonomously is forbidden. | 2026-07-02 |

## STANDING CONSTRAINTS (from Prime Directives — restated for quick check)

- D1: independent variable ∈ {triangle set/topology, triangle params, losses, supervision data, budget schedule}. NO selector/gate/threshold experiments (1 protocol-regression gate per module allowed).
- D3 floors: rendering +0.10 dB PSNR or 0.005 LPIPS; compaction ≥20% tri reduction at iso-quality (ΔPSNR ≥ −0.10, ΔLPIPS ≤ +0.005); geometry ≥20% relative. Below floor = DIAGNOSTIC.
- D4: run `tools/audit_test_path.py` in every /goal that reports numbers (exists after M1).
- D5: all numbers from `run_eval.py` (exists after M1). Until then, only legacy `metrics.py` numbers, labeled LEGACY.
- D6: preflight `df -h` before every run; abort if target volume < 50 GB. No >1h jobs in `/dev/shm`. Resumable long runs. Retention: latest + milestone checkpoints only.
- D8: Stage One scope = {toy_parking, dev_real_A, dev_drive_A} × B ∈ {50%, 25%}. No full9 sweeps, no holdout.
- Sunset rule: 3 consecutive below-floor results on one mechanism → tombstone here, close thread.

---

## MILESTONE STATUS BOARD

| Milestone | Status | % | Blockers / notes |
|-----------|--------|---|------------------|
| M0 Reproduce & Audit (AT0) | **DONE** | 100 | Eval reproduced exactly (garden 24.7120/0.7618/0.2163); tri counts censused; preflight tool demonstrated (caught 2 violations); FT cost measured 19.3 it/s; storage blocker escalated (DEC-007) |
| M1 Protocol & Harness (AT1) | **DONE** | 100 | All metric families proven: GT-model calibration row ≈perfect (g1 0.028%, g2 2.8mm, chamfer 1.8cm, d2 agreement 1.0) vs toy clean30k poor (g1 23.9%, g2 0.26m, chamfer 0.57m, d2 0.625) → metric code validated; toy clean geometry genuinely unreliable (headline motivation) |
| M2 Budget Engine (E1) | **CONCLUDED — E1 FAIL as written; mechanism VALIDATED; escalated** | 95 | KILL_REPORT.md filed. Garden B50 prune+features-FT BEATS clean (+0.157 dB, −0.0071 LPIPS, CIs excl. 0); garden B25 iso (75% reduction); courtyard B50 iso; toy −0.52 residual (geometric). Human decision pending: amended E1′ vs strict stop |
| M3 Geometry Objectives (E2) | **TOMBSTONED — demoted to evaluation-only** | 100 | 2 pre-registered FAILs + structural closure: BOTH sanctioned loss routes are blocked in this representation (opacity floor ∈[0.999,1] forbids transmittance/fade route; position motion breaks appearance for the depth-penalty route — measured repeatedly). Final variant unspent — closed on structural grounds, not exhausted budget; losses remain in-tree (default-off) + g1–g4 remain eval metrics. Floater-PRUNE alternative parked for human review |
| M4 Teacher Distillation (E3) | **TOMBSTONED by sunset rule — demoted to diagnostic-only** | 100 | 3 consecutive below-floor results (+0.039 → +0.051 → +0.125 dB garden distill−control, all CIs excl. 0 — REAL but sub-floor; toy unstable). Teacher factory + augmented-dataset machinery stays (train-only, D4-pure). Best garden compact model overall came from attempt-1 distill: +0.207 dB over clean at 50% tris |
| M4 Teacher Distillation (E3) | NOT STARTED | 0 | depends M2; teacher-render-loss hook already exists in train.py |
| M5 Downstream Proxy & Efficiency (AT5) | DONE (scoped) | 95 | d1/d2 computed on toy for every model row; FPS/VRAM/disk in every metrics.json + report table; compact models strictly cheaper. Gated remainder: courtyard d1/d2 (z_band up-axis derivation, documented) |
| M6 Integration & Report (AT6) | **DONE — awaiting human gate** | 100 | gems_pipeline = one command/(scene,budget,mode), stamp-resumable, config-hashed; STAGE1_REPORT.md written; fresh-clone repro PASS (Δ 0.0005 dB); audit green on all reported rows. NOTE: repro attempts 1–2 died silently (external SIGKILL, cause undetermined; improperly detached launches suspected); attempt 3, properly detached, passed |

## ITERATION BUDGETS & SUNSET WATCH

| Mechanism | Tuning-flavored goals used (max 2) | Consecutive below-floor (sunset at 3) | Status |
|-----------|-----------------------------------|----------------------------------------|--------|
| (none yet) | — | — | — |

---

## KEY VERIFIED FACTS (asset survey, 2026-07-02 — details in ASSET_MAP.md)

1. **Post-prune fine-tuning already exists**: `scripts/car_model/meshsplatopt_run_strict_compact_recovery.py` = topology-frozen continued training (`--load_iteration`, `--freeze_topology_updates --skip_restricted_delaunay`, `--densify_until_iter = load_iteration`). The legacy *pruner* itself (`ss3dm_prior/meshsplatopt/checkpoint_compaction.py`) does pure tensor surgery, NO optimization. So E1's "prune WITHOUT fine-tune ≈ legacy behavior" row = pruner alone; the recovery script is the existing FT recipe.
2. **Importance machinery exists**: CSEF selector `ss3dm_prior/meshsplatopt/compact_selector.py` (modes incl. `random_same_count` — usable for E1's random-prune row). On clean checkpoints only `render_contribution`=importance is populated; sparse/normal/free-space/debt terms are dormant → E1 importance definition should populate them from `utils/triangle_sparse_support.py` + `ecsr_build_surface_evidence_cache.py`.
3. **Teacher distillation hook exists**: `train.py --enable_teacher_render_loss --teacher_render_dir`; baking of ELA/Phase-J teacher renders via `scripts/car_model/meshsplatopt_apply_evidence_lumigraph_adapter.py`; orchestration example `ecsr_run_phaseg_teacher_bake_recovery.py`. ELA (`utils/evidence_lumigraph_adapter.py::adapt_frame`) can render arbitrary novel poses from train-only evidence (needs base render+depth at pose + train residual/depth caches) — pseudo-view factory is plumbing, not research.
4. **No mesh-space free-space loss exists** (only occupancy-space `free_space_violation_loss` in ss3dm_prior, unused by mesh optimizer). Renderer returns `expected_depth`, `surf_depth` (median), `rend_alpha`, `rend_normal`, `rend_ids` per pixel → both sanctioned M3 routes are implementable.
5. **Checkpoint format**: `<model>/point_cloud/iteration_N/point_cloud_state_dict.pt` with `triangles_points`, `_triangle_indices` (count implicit in shape), `vertex_weight`, `sigma`, per-vertex SH (`features_dc/rest`), `importance_score`. Eval mouth today: render.py → `test/ours_N/{renders,gt}` → metrics.py (PSNR/SSIM/LPIPS-vgg) → results.json.
6. **Data**: all 9 mipnerf360 scenes at `/data/peilincai/mesh_datasets/mipnerf360/`; clean full9 checkpoints at `outputs/carnet/meshsplatopt/paper_m360_repro/official_clean30k/` (23G). SS3DM ABSENT. ETH3D courtyard + T&T barn/truck + parking_phone_tiny available as dev_drive_A fallbacks.
7. **Settled post-mortems** (do not re-litigate): selector/arbitration dead (oracle headroom +0.0086 dB); static baking dead (4.76% gap capture, cos 0.21); Phase-J teacher = +1.33 dB upper bound; honest gates converge to no-op; micro-delta trap → D3.

## OPEN RISKS

- R1: `/data` at 100% (42 GB free) — every run writing under the repo's `outputs/` risks death-by-quota (v168 pattern). Mitigation: DEC-001 output root on `/` volume; preflight tool in M0. 2.2T of `outputs/` is historical — cleanup needs human approval, flag in report.
- R2: SS3DM absent → `dev_drive_A` degraded; g4 (Chamfer vs GT mesh) may only be possible on toy_parking (+ ETH3D courtyard if its laser GT is on disk). PROTOCOL must scope g4 honestly.
- R3: per-scene training wall-clock unknown (logs empty) — estimate in M0 from a short timed run before committing to fine-tune budgets.
- R4: toy_parking must be ingested via Blender/transforms_train.json path or synthetic COLMAP; Blender reader forces white bg / random init cloud if no points3d.ply — supply a GT-derived init cloud to avoid junk init.

---

## GOAL LOG

### GOAL #008 — Stage-1 completion under human delegation [M2/M3/M5] — 2026-07-02 — IN PROGRESS
- **HUMAN AUTHORIZATION (2026-07-02)**: user delegated best-judgment execution ("按照你认为最好的方式来"), explicitly following my recommendation to (a) adopt **E1′** (KILL_REPORT.md fallback), (b) sanction the **floater-prune mechanism as E2 variant 3/3** (topology edit; previously human-gated), (c) run the **26k-sourcing probe**; conclude Stage One when complete, then proceed to `docs/GEMS_Stage2_Prompt.md`.
- **Pre-registration A (E1′ verdict)**: criteria as in KILL_REPORT.md §fallback. Missing datum: courtyard B50 random_ft row (launching, tag e1b). (iii) on courtyard is at risk (ft−noft mean −0.082, 5 views) — will report CI honestly; E1′ passes/fails as computed, no re-thresholding.
- **Pre-registration B (26k-sourcing, DIAGNOSTIC→config candidate)**: hypothesis: garden B50 importance+features-FT sourced from clean@26000 ≥ 30k-sourced row (24.851) + 0.10 dB (CI excl. 0). Single run, tag e26src. If confirmed → recommended recipe for Stage 2 (not retroactive).
- **RESULTS (all three cards played to verdict, 2026-07-02):**
  - **E1′ as drafted: FAIL.** (i) courtyard importance_ft vs random_ft (matched features-only config) = +0.130 CI[−0.551,+0.640] ≪ +1.0 bar — with safe FT on a 33-view scene, random-prune damage is almost fully repaintable (random_ft 17.436 vs random_noft 16.209: FT recovered +1.23 dB). Importance's edge is decisive at aggressive budgets / no-FT (B25 random collapses 3–5 dB), not at B50-with-FT on small scenes. (ii) 2/3 scenes ✓. (iii) courtyard ft−noft −0.082 (mean below −0.05). No re-thresholding: E1 stays FAIL under both the original and the amended criteria.
  - **26k-sourcing: hypothesis REFUTED** — 24.8311 vs 30k-sourced 24.8509, diff −0.020 CI[−0.030,−0.010]. The features-only FT is itself the end-phase-damage recovery mechanism; source choice adds nothing. DIAGNOSTIC; recipe recommendation dropped.
  - **E2 variant 3 (floater-prune): FAIL with a high-value falsification** (evals `eval/e2v3_*`, diagnostics `eval/e2v3_*_prunedNoFT_diag`): g3 → 0 (−100%, bar met) BUT PSNR guard violated both scenes (toy −0.513 CI[−1.42,−0.04], courtyard −0.235 CI[−0.40,−0.11]); ~95% of the loss is the removal itself; toy view 00035 −8.07 dB from a genuine train-coverage gap (removed tris: max support 3 even over ALL 72 views). **Pre-registered prediction falsified: support≤1 triangles are NOT near-invisible — low-train-support content is precisely what test-like viewpoints see (selection effect). This also explains toy's B50 prune residual.** g1 moved ≤1.2% — floaters don't drive free-space violations. E2 thread closed at 3/3 variants incl. the sanctioned topology mechanism; demotion FINAL.
- **Pre-registration C (E2 variant 3/3, human-sanctioned: EVIDENCE-BASED FLOATER REMOVAL)**: mechanism = remove the PROTOCOL-g3 floater components (every member triangle train-support ≤ 1, component < 10k tris; computed from ≤60 train-view rend_ids pass on the M2 B50 model) via keep-mask compaction, then features-only FT 5k. Count stays ≤ B (removal only). **E2 PASS iff on toy AND courtyard B50: g1 OR g3 improves ≥30% relative vs the M2 model, ΔPSNR ≥ −0.10 dB (paired CI), AND before/after floater panels show visible cleanup.** Predicted: g3 −~100% by construction-adjacent (removal targets exactly the metric's population — legitimate: the metric measures unsupported junk and the fix is removal), g1 down where floaters blocked evidence rays, ΔPSNR ≈ 0 (support≤1 triangles are near-invisible in renders).

### GOAL #007 — M4 teacher distillation, TEST E3 [M4] — 2026-07-02 — IN PROGRESS
- **E2 variant-1 (gradient routing) FAILED** (evals `eval/m3v1_*_B50_v1`): toy dPSNR −1.212 CI[−1.75,−0.71] guard blown, g1 +1.2%, g3 −6.2%; courtyard dPSNR −0.752 CI[−1.32,−0.24] guard blown, g1 +5.0%, g3 −14.1%. Kill condition met. E2 now 2 consecutive FAILs; final sanctioned variant DEFERRED (low-plausibility λ/length tweaks ≈ threshold-fiddling); structural diagnosis in board. Floater-REMOVAL via free-space evidence (representation-level, D1-legal) parked as M2-stretch proposal — needs human review since E2's letter specifies losses.
- **Pre-registered E3 hypothesis**: mechanism = train-only teacher distillation into ALL triangle parameters via PSEUDO-VIEW DATASET AUGMENTATION: (a) pseudo-poses = leave-k-out over train views (k≈12%, teacher for a held-in pose uses ONLY the remaining train evidence) + pose jitter/interpolation near the train trajectory (≈1× train count total); (b) teacher = ELA/Phase-J `adapt_frame` at each pseudo-pose (base render+depth from the B50 model at that pose; support residuals from train views ONLY — D4-pure by construction, asserted in code); (c) distillation = pseudo-views added as EXTRA TRAIN IMAGES (their GT := teacher renders) in an augmented dataset; features-only fine-tune (the validated safe channel) on real+pseudo train views; no train.py changes needed.
- **E3 PASS iff** at B=50% on ≥2 dev scenes (garden + toy primary): ≥ +0.15 dB PSNR or ≥ 0.008 LPIPS vs the same model fine-tuned WITHOUT teacher views (= the M2 features-FT e1v2 rows), CI excl. 0. Also report recovered fraction = gain/(Phase-J − clean). 3-variant rule (pseudo-view density/source; loss form; curriculum).
- Predicted effect: garden ≥ +0.15 dB (teacher headroom +1.33 dB full9 precedent); toy uncertain (synthetic; teacher quality bounded by ring coverage).
- Teacher factory built + adversarially reviewed (commit 694024b): leave-k-out teacher beats base 6/6 diagnostics (garden +0.79 dB mean, toy +2.35); D4 triple-enforced; datasets_aug/{garden,toy_parking}_B50_teacher (341/153 train incl. 180/81 pseudo; original test sets exact). Bake cost: 4.5 min garden.
- **E3 attempt 1 (density ~1×, features-only distill, same-length no-teacher CONTROL runs as primary comparison): below floor — DIAGNOSTIC.** Evals `eval/e3_*_v1`: garden distill−control **+0.0391 CI[+0.0159,+0.0616]**, dLPIPS −0.0015 CI excl. 0; toy **+0.1149 CI[+0.0529,+0.1908]**. Both positive with CIs excl. 0 → the distillation channel is REAL but under the +0.15 dB / 0.008 LPIPS floor. Garden distill vs clean: **+0.2072 CI[+0.1475,+0.2617] at 50% triangles** (best compact row yet; run `eval/e3_garden_B50_distill_v1`). Recovered fraction (proxy): garden 0.039/0.79 ≈ 5%; toy 0.115/2.35 ≈ 5%.
- **E3 variant 1/3 (pre-registered before launch): pseudo-view DENSITY ×3** (—jitter-count-frac 1.5 —interp-count-frac 1.5, k-out 0.12 unchanged; ~3× train count total; same FT config; same controls). Predict: distill−control ≥ +0.15 dB on both scenes (effect scales with pseudo coverage). Kill: if <+0.10 on garden after tripling, density saturates → variant 2 = teacher source/strength dial.
- **E3 variant 1 result: kill condition met (density saturates).** Evals `eval/e3v1_*`: garden distill−control +0.0514 CI[+0.0280,+0.0740] (tripling added +0.012); toy +0.1325 CI[+0.0535,+0.2307] (<0.15). Garden distill vs clean now +0.2195 at 50% tris. Capture ≈5–7% of teacher headroom on both — consistent with the v1xx post-mortem (Phase-J residual is VIEW-CONDITIONED; view-independent features_dc can't hold it; held-out residual cosine was 0.21).
- **E3 variant 2 result → SUNSET (thread closed)**: garden distill−control +0.1251 CI[+0.0759,+0.1727] (best yet, still < +0.15 floor; dLPIPS −0.0022 < 0.008); toy +0.0261 CI[−0.146,+0.187] AND the rest-lr ×20 itself damaged toy (control 29.93 vs 30.33 at default rest-lr; LPIPS 0.21 vs 0.11 — SH overfits 72 views). Three consecutive below-floor results on the distillation mechanism → PROTOCOL sunset rule → tombstone. Teacher demoted to diagnostic-only (M4 failure path). Evals `eval/e3v2_*`. Note: every distill−control delta was POSITIVE with CI excl. 0 on garden (real channel, view-conditioning-capped at ~5–16% of teacher headroom — quantitatively consistent with v1xx post-mortem cos≈0.21).
- **E3 variant 2/3 (pre-registered before launch): distill into VIEW-DEPENDENT SH** — features_rest lr raised from feature_lr/20 to feature_lr (new knob `--feature_rest_lr_mult 1.0`, default 0.05 = historical behavior; surgical change in triangle_model.training_setup). Same density-×3 datasets, same controls... CONTROLS MUST BE RE-RUN with the same rest-lr (else the comparison confounds rest-lr with distillation): control = original dataset, feature_rest_lr_mult 1.0, 40k→50k. Mechanism: view-conditioned teacher signal can land in SH deg 1–3; distinct from dead v1xx baking (frozen geometry, DC carriers, no pseudo-view coverage). Predict: distill−control ≥ +0.15 dB garden AND toy. Kill: garden < +0.10 → E3 capped by view-conditioning; tombstone with DIAGNOSTIC positives; escalate (Stage One would then need E2-final-variant or human decision). — M3 geometry objectives, TEST E2 [M3] — 2026-07-02 — IN PROGRESS
- Human question on E1′ amendment went unanswered (60s timeout); proceeding under "E1 stays FAIL, Stage One continues" — the no-amendment option; final go/no-go remains human's. E1′ decision remains OPEN in KILL_REPORT.md.
- **Pre-registered E2 hypothesis**: mechanism = add (1) free-space hinge loss L_fs = mean relu(0.95·d_evidence − D_rendered)/d_evidence over sampled train-view evidence (toy: GT train depths; courtyard/garden: COLMAP sparse train points — train-only, D4-pure) via the DIFFERENTIABLE rendered depth, and (2) multi-view rendered-depth consistency loss (pairwise warp between train views, occlusion-masked) — both integrated into the M2 fine-tune (features default lr + positions at low lr re-enabled: geometry losses supply coherent position signal, unlike the noise-drift case; weights frozen). Implementation route: rendered-depth penalties (Route B) — justification: rasterizer already backprops through depth outputs (train.py's existing vertex-depth loss proves it); no CUDA changes.
- **E2 PASS iff** at B=50% on toy_parking AND courtyard: g1 OR g3 improves ≥30% relative vs the M2 model (CI excl. 0), ΔPSNR ≥ −0.10 dB vs the M2 model, AND before/after panels show visible floater/free-space cleanup. Kill: 3-variant rule; if dead → geometry axis demoted to evaluation-only (documented, claim edited, human review).
- Baselines (the "M2 model" at B50): toy = toy_parking_B50_importance_ft_e1v2; courtyard = courtyard_B50_importance_ft_e1v2 (launched now, features-only FT 10k — also completes M2 trend table + E1′(i)(ii) courtyard data).
- Predicted effect: toy g1 0.202→≤0.14; courtyard g1 0.094→≤0.066 (or g3 equivalents); rendering held within −0.10 dB.
- **E2 attempt 1 (base config, λ_fs=0.01/λ_dc=0.05/pos-lr 1.5e-5): FAIL both scenes** (evals `eval/m3_e2_{toy_parking,courtyard}_B50_v1`): toy g1 −1.0%, g3 frac −21.8% (<30%), PSNR guard VIOLATED (−0.245 CI[−0.43,−0.09]); courtyard g1 +2.0%, g3 −9.6%, guard ok (−0.056 CI incl. 0). Loss trajectories: L_fs barely decreases on toy → geometry signal too weak vs mobile-position photometric drift (the E1 mechanism returned once positions re-enabled: toy's −0.245 at weak λ is drift, not geometry-loss cost).
- **E2 mechanism variant 1/3 (pre-registered before implementation): GRADIENT-ROUTED geometry coupling** — photometric loss backprops to FEATURES only; geometry losses (λ_fs=0.05, λ_dc=0.10) backprop to POSITIONS only (two-pass backward, vertices.grad cleared between); weights frozen; position lr 1.5e-4 (×10, safe because positions no longer receive photometric noise); 10k iters; same config both scenes. Predict: g3 floater fraction −30%+ (positions free to evacuate free space without photometric counter-pressure), g1 down on toy, PSNR guard held by features-only photometric channel (the E1-v2-validated safe channel). Kill: guard violated on either scene while g-metrics still <30% on both.

### GOAL #005 — E1 budget engine runs [M2] — 2026-07-02 — CONCLUDED (see KILL_REPORT.md)
- **Variant 3 result**: garden iterative B50 = 24.8689 (+0.157 vs clean CI[+0.110,+0.200], ΔLPIPS −0.0071) — best row yet; toy unchanged vs one-shot (−0.022 CI[−0.115,+0.079] vs noft; −0.542 vs clean) → toy kill condition tripped. Variants 3/3 spent.
- **FINAL VERDICT: E1 FAIL as pre-registered (criterion (b) miscalibrated — presumed lossy pruning; measured prune-only −0.011 dB garden B50; criterion (a) fails on toy only). Mechanism (evidence-prune + features-only FT) VALIDATED on real scenes. Full accounting + fallback proposal in `KILL_REPORT.md`. Escalated to human: adopt E1′ or strict stop. M2 experiments STOPPED per rules; no soft pivot.**
- **E1 attempt 1 (tag e1b, post scaling-fix) = FAIL as pre-registered** (`/data/peilincai/gems_stage1/analysis/e1_summary.{md,json}`): importance_ft LOSES vs importance_noft on both scenes (garden B50 −2.92 dB CI[−3.29,−2.54]; toy B50 −1.26 CI[−1.79,−0.78]). But the FAILURE STRUCTURE is informative:
  - **Prune-only (importance=pixels_total) is near-lossless: garden B50 −0.011 dB CI[−0.033,+0.003], ΔLPIPS +0.0001 → MEETS the D3 compaction floor (50% tris, iso-quality, 32→42 FPS).** garden B25 −0.139; toy B50 −0.520; toy B25 −1.167 (fail strict floor). Random-prune is far worse at B25 (garden −5.0 vs clean) → importance definition genuinely matters.
  - **The FT stage damages ANY resumed model** — diagnostics (all toy, durable evals): D-a clean-resume+10k FT, NO prune → −1.12 dB (`eval/toy_cleanresume_diag`); D-b prune+FT-1k → only −0.11 vs noft (`eval/toy_parking_B50_importance_ft_diag1k`) → damage ∝ FT length; D-c fine-tuned model loses 3.7 dB on ITS OWN TRAIN VIEWS (36.22→32.53) and its training loss RISES ~50% over 10k iters → not overfitting; optimizer noise-drift at default constant LRs (feature 0.0016, weight 0.03) on a converged model. Supersampling fix verified effective (model best at scaling 4). Sigma/floor/pos-LR schedules verified resume-consistent.
- **Mechanism variant 1/3 (pre-registered before launch)**: drift-controlled FT — feature_lr×0.1 (0.00016), weight_lr=0, positions unchanged, 10k iters. Predict: toy B50 ft−noft ≥ 0 (recovers part of −0.52); garden B50 |ft−noft| < 0.1 (no damage). Kill: FT still degrades ≥0.2 dB on both. Tag e1v1, 4 rows (2 scenes × B50/B25), launched.
- **Variant 1 result: FAIL, hypothesis falsified** — LR-reduction made damage WORSE (garden B50 ftv1 −4.13 dB vs clean, CI[−4.57,−3.67]; both garden budgets converge to the same degraded PSNR 20.58 → systematic attractor, not noise). Evals: `eval/*_importance_ft_e1v1`.
- **Root-cause chain (probes, durable in logs/this entry):** (1) fine-tuned model's TRUE train loss DOUBLES (0.004747→0.009394 over 72 views) → optimizer ascends its own objective; (2) single-view repeated stepping DESCENDS cleanly → gradients correct; (3) pure-photometric multi-view loop (none of train.py's extra terms) still ascends (0.004747→0.005351 @3k) → core stochastic dynamics; (4) channel isolation @1.5k steps: vertices-only +0.000670 (DESTROYER, even at lr 1.5e-5), features-only +0.000169 (mild, acts as repairer of position damage — explains v1 < default), weights-only −0.000001 (innocent); (5) fused_ssim NOT installed → toy trained today with identical utils-ssim objective as FT → not an objective switch. Interpretation: near convergence, Adam-normalized position updates drift the model along a value-destroying direction; the trainer's own end phase is on this slope (test: garden@26000 vs @30000 eval running).
- **Mechanism variant 2/3 (pre-registered before launch)**: features-only FT — position lr 0, weight lr 0, features default 0.0016, 10k iters, tag e1v2. Predict: toy B50 ft−noft > 0 (CI excl. 0, recovers part of −0.52); garden B50 |ft−noft| ≤ 0.1. Kill: degradation ≥0.2 dB again on both.
- Courtyard prune-only rows done (tag e1b, incl. random_noft); garden/toy random_noft rows done (M6 trend baselines).
- **Variant 2 result: MECHANISM WORKS on real scene** (evals `eval/*_e1v2`, CIs by PROTOCOL bootstrap): garden B50 24.8509 = **+0.139 dB vs clean CI[+0.097,+0.177] AND ΔLPIPS −0.0061 CI[−0.0065,−0.0057] at 50% triangles** (clears D3 improvement + compaction floors); garden B25 iso vs clean (+0.027 CI[−0.054,+0.099], ΔLPIPS −0.0014) at 25% triangles (clears compaction floor, 75% reduction); vs noft +0.150/+0.166 CI excl. 0. Toy: FT harmless but no recovery (B50 −0.004 vs noft; −0.524 vs clean persists — prune damage is geometric; features can't rebuild it).
- **Key context discovered: garden@26000 = 25.029/0.780/0.201 BEATS garden@30000 = 24.712/0.762/0.216** (eval `eval/garden_clean26k_diag`) — original training's final ~4k iterations were already value-destroying (same position-drift mechanism); explains historical clean26000 references. Features-only FT partially recovers this pre-existing damage on the remaining triangles. NOTE for Stage-2/human: sourcing compaction from the 26000 checkpoint may be even better; NOT explored (variant budget + DEC-005 frozen baseline).
- **Mechanism variant 3/3 (pre-registered before launch)**: iterative prune schedule — 2 steps: prune to ~70.7% + features-only FT 5k, then prune to exactly floor(0.5·T_clean) + features-only FT 10k (B25: two more equivalent steps). Same per-step config both scenes (D7). Predict: toy B50 full-pipeline vs clean better than one-shot's −0.52 (target ≥ −0.20 = E1(a)); garden must stay ≥ clean (no regression vs one-shot v2). Kill: toy still < −0.35 vs clean or garden regresses below iso → E1 FAIL final, verdict written with variant-2 results as the M2 outcome.
- Iteration budget: E1 mechanism variants used 3/3 (this is the last). Retention: diag ckpts deleted, evals kept.
- Pre-registered hypothesis (before any run): evidence-guided prune to B + topology-frozen FT (10k it, frozen config) → at B=50 on garden AND toy: ≥ +0.5 dB vs prune-no-FT (CI excl. 0) AND ≥ −0.20 dB vs clean. Kill: either fails on either scene after ≤3 mechanism variants.
- 12 runs launched as 3 detached GPU chains (tag e1). CRASH #1: all importance-mode runs died at `gems_pipeline.py:519` — `stage_evidence` returned the npz path (str) where the stamp payload (dict) was expected; random-mode smoke never exercised the branch. Root-caused, fixed (commit ee5cf64), chains relaunched; stamps resumed completed work. No results were affected (crash was before FT).
- CRASH/VOID #2: first courtyard g4 row (courtyard_clean30k_v2) showed F@5cm=0.009 → investigated → ETH3D raw scan vertices are ~1.2 m off the camera frame; `scan_alignment.mlp` transforms are REQUIRED (verified: median sparse→scan 0.036 m transformed vs 1.19 m raw; camera frames of eth3d_colmap and ETH3D GT calibration are bit-identical). Fix: transforms frozen into scenes.py, loaders apply them, ROI recomputed from transformed AABB, PROTOCOL changelog 1.1.1, v2 g4 row VOID (commit cb1560b). v3 eval re-running.
- Courtyard clean baseline (trained this session, 30k it, ~20 min, GPU 5): PSNR 17.686 / SSIM 0.5968 / LPIPS 0.3850, 4.37M tris, 104.6 FPS; g1 9.40% (99,690 samples), g3 5808 comps / 0.81% — geometry much worse than garden, good E2 headroom. Eval: `/data/peilincai/gems_stage1/eval/courtyard_clean30k_v1` (+v3 for g4).

### GOAL #003 — M1: PROTOCOL v1.1.0 + eval harness, validated + adversarially reviewed [M1] — 2026-07-02 — DONE
- Infra goal. Built via 8-agent workflow (5 implementers, integrator, 2 adversarial reviewers) + 1 fixer agent.
- Artifacts: `PROTOCOL.md` (v1.0.0→v1.1.0 same-day pre-first-row amendments, changelog inside), `run_eval.py` (single mouth), `tools/gems/{scenes,eval_context,panels,geometry_metrics,downstream_metrics,paired_bootstrap,test_paired_bootstrap}.py`, `tools/audit_test_path.py`.
- Validation (durable): `/data/peilincai/gems_stage1/eval/garden_clean30k_v2/metrics.json` — PSNR 24.7120037 (bit-identical to legacy mouth), SSIM 0.76179, LPIPS 0.21626; g1=0.0045972 (99,626 SfM samples, 458 violations); g3=1941 floater comps / 0.13754% tris (60 support views); cost: 11.57M tris, 942MB, 6.9GB VRAM, 32.2 FPS; full eval 81 s. Bootstrap self-test: coverage 0.948, n=2M×10k resamples in 214 s / 1.59GB RAM, chunked==full-matrix bit-identical. Audit: GREEN (3443 modules, 4166 traced reads); hardened after review caught a real evasion hole (slash-form/bare-name car_model imports).
- Reviews: protocol-compliance + numerical-correctness. All blocking findings fixed & revalidated (chunked bootstrap; g4 ROI gate; surface=all-triangles per opacity floor; audit blocklist; voxel sampling guard). Deferred (documented, benign): per-view LPIPS construction (slow, bit-exact); g1 silhouette-edge bias (deterministic, model-invariant, in PROTOCOL).
- VERDICT: PASS.

### GOAL #004 — M1b: toy_parking dataset + clean baseline [M1] — 2026-07-02 — IN PROGRESS
- Infra goal. `tools/gems/build_toy_parking.py`: procedural GT scene (meters): 34×34m mottled ground + lot markings, 2 cars, 10cm pole, 8-post fence + rails, textureless wall, curb; GT mesh 54,513 V / 105,982 F; 90 views @1000×750 (72/18 file split), exact GT depth per view; COLMAP-text export verified against reader; element coverage ≥8 views PASS (min: pole 34).
- Renderer facts discovered (recorded in dataset_manifest.json): tile compositor sorts by triangle-center depth, NO backface culling → builder bakes per-view backface masks; screen-space culls (>1600px, <1px, inradius<1px) → tessellation ≤0.5m. sigma=1e-4 (log-stored) for hard edges; vertex_weight stored pre-sigmoid; render opacity = 0.999+0.001·sigmoid (floor pinned by load_parameters).
- Ingestion smoke 300 iters PASS (~93 it/s). Clean30k training detached on GPU 1 (PID 4050215, wandb run gems_toy_parking_clean30k, online). Toy ROI frozen in scenes.py; z_band[0] corrected 0.1→0.0 (true ground) pre-first-row.
- DONE 2026-07-02: training completed (30k iters, ~17 min, final model 6,590,559 tris / 541 MB — 62× over-parameterized vs GT 105,982 faces; contains 13 NaN faces, handled per PROTOCOL §4.3 non-finite exclusion added pre-first-row).
- **M1b acceptance evidence** (durable): `/data/peilincai/gems_stage1/eval/toy_parking_clean30k_v1/metrics.json` and `.../toy_parking_GTmodel_v1/metrics.json`:
  - toy clean30k: PSNR 30.894 / SSIM 0.9603 / LPIPS 0.0936; 62.3 FPS; g1 23.85% (360k GT-depth samples), g2 0.2559 m, g3 395 floater comps (0.28%), g4 chamfer 0.5696 m / F@5cm 0.396, d1 false_free 58.9% / false_occ 3.0%, d2 agreement 0.625 (unsafe_disagreement 0.0).
  - GT-mesh model (calibration): PSNR 56.17; g1 0.028%, g2 0.0028 m, g4 chamfer 0.0179 m / F 0.998, d1 0.19%/0.01%, d2 agreement 1.000 → **metric code validated end-to-end; clean-model geometry unreliability is REAL** (motivates E2 and compaction headroom for E1).
- VERDICT: PASS (AT1 + M1b complete).

### GOAL #002 — M0 completion: verified reproduction, census, costs, storage [M0] — 2026-07-02 — DONE
- Infra goal. Evidence (durable): `/home/peilincai/gems_stage1/{m0_triangle_census.txt, logs_m0_garden_e2e.log, m0_repro/garden/results.json}`.
- **Eval path GREEN & exact**: render.py + metrics.py on copied garden ckpt (GPU 3) → PSNR 24.7120 / SSIM 0.76179 / LPIPS 0.21626 = recorded baseline (24.71/0.762/0.216). Render: 24 test views in 34 s incl. PNG I/O (×4 supersampling).
- **Trainer GREEN**: 150-iter topology-frozen fine-tune from garden@30000: **19.3 it/s** (11.57M tris, images_4). → 10k-iter fine-tune ≈ 9 min/scene. Full-train 30k ESTIMATE ≈ 40–80 min/scene (rate varies over training; not directly measured — logs empty; toy scene will be timed in M1b).
- **Triangle census (30000 = 26000, topology frozen)**: bicycle 9.42M, flowers 9.65M, garden 11.57M, stump 9.28M, treehill 9.53M, room 11.17M, counter 9.85M, kitchen 9.72M, bonsai 10.83M; verts 2.45–3.61M; .pt 743–988 MB; SH deg 3. → B=50% ≈ 4.6–5.8M tris, B=25% ≈ 2.3–2.9M.
- `tools/storage_preflight.py` written + demonstrated: PASS on quota-free target, FAIL on `outputs/` (41 GB < 50). Then the FT-probe's final save **died on the home quota** (torch.save mid-write) — which is how DEC-007's quota was discovered. Crash root cause: 100 GiB uid quota on `/`; fix: deleted my ckpt copies (~2 GB), rerouted all GB-scale work to pending DEC-007.
- Env correction: mesh_splatting env is Python **3.11** (not 3.10).
- VERDICT: PASS (AT0 satisfied; training demonstrated via resume-probe per DEC-005 justification).

### GOAL #001 — Bootstrap [M0] — 2026-07-02 — DONE
- Infra goal (no hypothesis). Created LEDGER.md, ASSET_MAP.md (pre-verified draft), internalized §2 context via 3 parallel code surveys (train/render/eval path; compaction+ELA+evidence; data/checkpoints/storage).
- Storage preflight (manual): /data 42G free ⚠ BELOW 50G FLOOR; / 5.9T free ✓; /dev/shm full ✗. → DEC-001.
- SS3DM verified ABSENT → DEC-003.
- Next: GOAL #002 — complete M0/AT0: triangle counts from .pt, executed end-to-end render+metrics on garden clean checkpoint, `tools/storage_preflight.py`, wall-clock estimate, dev_drive_A candidate verification (ETH3D GT availability), finalize ASSET_MAP.

### GOAL #009 RESULT (2026-07-03) — E1-PARETO S-REND B50 slice: PREDICTION MET, SUPPORTS C1+C3
- 8 new scenes evaluated vs same-mouth clean rows (evals `eval/<scene>_{clean30k_v1,B50_importance_ft_s2,B50_importance_noft_s2}`): B5 within −0.10 dB on 7/8 (predicted ≥7); ABOVE clean with CI excl. 0 on bicycle/kitchen/bonsai/stump; LPIPS improved 8/8. Full9 incl. garden: 8/9 within −0.10, 5/9 above clean, 9/9 LPIPS better at half triangles. flowers FAILS (−0.147 CI[−0.31,−0.00]; also B4 −0.238) → E9-FAIL taxonomy candidate (hard low-texture scene; prune damage not FT-recoverable, cf. toy selection-effect).
- B25 batch launched (same chains/tag s2), B12.5 to follow; D-1 SS3DM acquisition = next blocking cell.

## STAGE 1R INSERT (docs/GEMS_Stage1R_Insert_Prompt.md, activated 2026-07-03)

### GOAL #R-00 — R0 sequencing + pre-registrations — 2026-07-03 — IN PROGRESS
- R0 applied: in-flight B25 batch (supervised jobs b25_gpu{1,2,3,5}) runs to completion; NO new anchor-dependent comparative rows (H1/R1-3DGS/S-GEN claims) until R1 verdict; D-1 SS3DM acquisition launched NOW (background agent, I/O only); iteration-budget lines opened: R1 (0/2 policy variants), E2R (0/2 variants), R3.a (0/2).
- **R1.b ANCHOR POLICY PRE-REGISTERED (frozen now, before any non-garden 26k numbers seen): Option (ii) — clean-fixed@30k**: from each scene's 26000 checkpoint, re-run iterations 26000→30000 with the validated safe channel (features-only: --lr_triangles_points_init 0.0 --weight_lr 0.0, otherwise frozen config). One rule, all 9 S-REND scenes (D7). R1-T: clean-fixed@30k ≥ clean@30k PSNR on ≥7/9 scenes with paired CI excl. 0 → PRIMARY anchor; else clean@30k stands (honest either way). R1.a: clean@26k eval rows also produced for dual-row tables. toy/courtyard have no 26k saves (default save schedule) → anchor policy applies to them by regenerating the 26k-equivalent AFTER the S-REND verdict (documented deviation: R1-T is defined on S-REND only).
- E2R-v1 pre-registration (before code, per R2): learnable per-triangle opacity o=sigmoid(logit) on [o_min,1], **o_min=0.01** (justification: keeps every triangle faintly renderable so the photometric gradient can rescue load-bearing content — a pure 0 floor allows dead-gradient lock-in; 0.01 alpha is visually negligible); logits init to reproduce current renders bit-for-bit at step 0; opacity L1 decay regularizer λ_o (single frozen value, pre-registered at 1e-4 after scale sanity-check vs photometric loss magnitude — the sanity check may adjust the exponent BEFORE first real run, never after); fade-and-prune: o<τ_fade=0.05 for T=3 consecutive 500-iter checks → removed at next prune step; FT from B5@50 models, channel {features, opacity}, positions frozen, 10k iters. Compositing-order probe REQUIRED before full runs. PASS bar per R2. Variants 0/2 used.
- **D-1 RESULT (2026-07-03): ACQUIRED** — SS3DM Town{01,02,03,06}/150_streetsurf (150 frames × 6 cams × 5 lidars each) + GT town meshes at /data/peilincai/mesh_datasets/SS3DM/ (~15 GB of the 137 GB distribution via Zip64 central-directory range extraction, 13,808 files CRC-verified; HF `SS3DM/SS3DM-Dataset`, CC-BY-NC-ND-4.0, no auth). Schema matches docs/ss3dm_prior_data_schema.md except scenario.pt = raw pickle (documented). S-GEO UNBLOCKED; next: ingestion converter (D-1b) + B0 trainings after R1.
- **B25 slice verdict (2026-07-03, 16 rows tag s2 + garden e1-era rows)**: B5@B25 meets the strict compaction floor on 3/9 (garden +0.027, bicycle +0.066, stump −0.049); elsewhere −0.20..−0.34 dB (flowers worst −0.34). B4@B25 uniformly worse than B5 (FT adds +0.06..+0.17 everywhere at this budget — FT value GROWS as budget shrinks, consistent with E1'/G4). SUPPORTS C1's budget-regime framing; BOUNDS the iso-quality claim to B50 (8/9) and B25-on-easy-scenes (3/9). Supervision protocol: all 4 jobs EXITED:0, zero babysitting.

### GOAL #R-01 — R1 anchor verdict + re-expression [R1] — 2026-07-03 — DONE
- R1.a/b: 9× clean-fixed@30k trained (features-only 26k→30k, one rule) + 18 anchor evals. **R1-T: 9/9 scenes fixed>clean30k, CIs excl. 0 (+0.034..+0.314 dB, LPIPS better 9/9) → clean-fixed@30k = PRIMARY ANCHOR.** Honest context: clean@26k > fixed@30k on all 9 (26k is a 4k-fewer-iterations anchor, not compute-matched; policy pre-registered, no per-scene choice). End-phase decline is thus NOT fully position-drift: features-only continuation also loses vs the 26k snapshot on test views.
- R1.c re-expression vs PRIMARY anchor: B50 B5 floor 4/9 (garden +0.070 CI excl. 0 STILL above anchor; bicycle/stump/treehill iso; indoor 4 + flowers fail at −0.13..−0.22); B25 floor 2/9. Legacy vs-clean30k rows retained as dual-row (deployed-default-recipe comparison). NOT reopening sourcing (boundary): B5-from-30k already embeds the features-only repair (garden B5 > fixed anchor), so indoor gaps are true prune cost.
- Iteration budget R1: 1/2 policy variants used. Jobs: 4/4 EXITED:0 under supervision.

### GOAL #R-02 — R3.a occupancy-extraction study: route (i) voxelization vs route (ii) TSDF fusion [R3.a] — 2026-07-03 — DONE (hypothesis FALSIFIED)
- **PRE-REGISTERED HYPOTHESIS (frozen before any route-(ii) number is computed):** route (ii) TSDF fusion of TRAIN-VIEW rendered median depths reduces toy d1 false_free_rate by ≥50% relative vs route (i) triangle voxelization, at ≤2× route-(i) false_occupied_rate, with fusion params calibrated ONCE on toy clean30k and FROZEN. PASS iff all 3 toy models (clean30k@30000, B50 ft e1v2@40000, B25 ft e1v2@40000) meet the bar with paired-bootstrap CI excl. 0 on the false-free difference (units = GT-occupied voxels). Courtyard (clean30k@30000, B50 ft e1v2@40000) = frozen-params transfer verdict, reported either way. SS3DM excluded (B0s still training).
- **Route (ii) definition (frozen):** voxel grid = the exact d1 `_VoxelGrid` (scenes.py ROI/z_band, 0.10 m). Per TRAIN view: render median depth (`surf_depth`, training-time settings via eval_context); projective TSDF at voxel centers, sdf = rendered_depth(px,py) − voxel_cam_depth; valid observation iff pixel in frame, in front of camera, rendered alpha ≥ 0.5 (same G1_ALPHA_MIN), finite depth > 0, and sdf > −tau_t (occlusion discard). Per-voxel aggregation: median of clamped-to-[−tau_t,+tau_t] samples; occupied iff n_obs ≥ min_views AND |median| < iso. Unobserved voxels = free (same semantics as route (i) unmarked). Train views only (D4); no per-view logic in the artifact — the fused grid is ONE global object per model.
- **Sanctioned ONE-TIME calibration grid (≤3×3×2, toy clean30k ONLY, table logged):** tau_t ∈ {0.20, 0.30, 0.45} m; iso ∈ {0.05, 0.10, 0.15} m; min_views ∈ {2, 5}. Objective (pre-registered): minimize false_free_rate s.t. false_occupied_rate ≤ 2× route-(i) false_occupied_rate on toy clean30k; tie-break lower false_occupied_rate. Then FREEZE and apply unchanged everywhere. Iteration budget R3.a: this is the sanctioned calibration, 0/2 tuning variants consumed.
- d2 on BOTH routes' grids: identical 200-trajectory machinery (`_sample_trajectories`/`_collision_verdicts`), sampler seed 0 unchanged, verdicts vs the same GT grid.
- Tool: `tools/gems/occupancy_routes.py` (wraps downstream_metrics grid/rasterization/GT/trajectory code — no metric fork). Durable outputs: `/data/peilincai/gems_stage1/analysis/r3a_occupancy_routes/`.
- **RESULT (2026-07-03): FALSIFIED — route (ii) moves d1 in the WRONG direction on every cell.** Calibration table (all 18 combos logged, all fo-feasible): route-(ii) ff 0.832–0.957 vs route-(i) ff 0.589 on toy clean; frozen best = {tau_t 0.20, iso 0.15, min_views 2}. Frozen-params cells (ff route-i → route-ii; fo multiple): toy clean 0.589→0.832 (×0.49), toy B50 0.597→0.832 (×0.51), toy B25 0.615→0.832 (×0.54), courtyard clean 0.650→0.839 (×0.35), courtyard B50 0.668→0.839 (×0.38). All paired CIs on ff_i−ff_ii exclude 0 on the wrong side (toy clean [−0.245,−0.239]). Toy bar: 0/3 PASS; courtyard transfer: same direction. d2 (seed 0, same 200 trajs): route ii slightly BETTER agreement (toy 0.650 vs 0.625; courtyard 0.925 vs 0.895), unsafe_disagreement 0.0 everywhere — both scenes' d2 errors are over-collision, which route ii's lower false-occupied reduces. Paper consumption ("how to consume mesh splats for planning"): triangle voxelization is the safer consumer on the safety-critical axis; median-projective-TSDF trades false-occupied for false-free — the wrong trade for parking.
- Mechanism (reported, not re-litigated): projective TSDF at 0.10 m voxel centers under grazing viewing geometry — a voxel center ~5 cm off a thin surface (ground, oblique walls) reads a large positive along-ray sdf (~offset/sin(elev)) from oblique cameras, so the per-voxel median votes free; route (i) marks any cell containing surface. Route-(i) cross-check: reproduces the stored eval d1/d2 rows EXACTLY on all 4 cells that had them.
- **Notable side finding:** fused route-(ii) grids are BIT-IDENTICAL between clean and B50 on BOTH scenes; verified upstream — `surf_depth` bit-identical per pixel between toy clean30k and B50@40000 (rend_alpha max diff 3.9e-4, never crossing the 0.5 gate); B25 differs (483/1.74M voxels). B50 evidence-importance prune + FT preserves the rendered median-depth surface exactly → depth-derived downstream artifacts are invariant to B50 compaction (supports C4-style preservation claims from a new angle).
- Discipline: pre-registration above frozen before any route-(ii) number existed; one-time calibration only, 0/2 tuning variants consumed; D4 green (train views only; no test data in any artifact); no git commits. Honest negative closure per insert R3.a ("either verdict is a paper result"). SS3DM pending B0s → R3.a re-run on SS3DM is a follow-up cell, not a variant.

### GOAL #R-03 — R3.c planner closed loop v0: Hybrid-A*-lite on R3.a occupancy grids [R3.c] — 2026-07-03 — DONE (P1 preservation PASS; P2 FALSIFIED as pre-registered; raw grids of BOTH routes fail closed-loop consumability)
- **PRE-REGISTERED MECHANISM (frozen before any plan is computed):** Hybrid-A*-lite planner = A* on an SE(2) lattice (cell 0.10 m = the d1 voxel, 16 heading bins) with straight + arc motion primitives: straights 0.5 m fwd/rev; arcs radii {4, 8, 20} m × {left, right} × {fwd, rev}, each arc sweeping exactly one heading bin (22.5°) → 14 primitives; cost = arc length × (1.0 fwd / 2.0 rev), no switch penalty; heuristic = max(Euclidean, obstacle-aware 2D Dijkstra distance × cos 22.5°) − 0.30 m goal tolerance (admissible). Collision model: per occupancy grid, footprint layer = z-band collapse over the d2 vehicle band [z_lo+0.1, z_lo+1.5] (identical to `_collision_verdicts`); ESDF = scipy 2D distance transform of that layer × 0.10 m; costmap lethal iff ESDF ≤ half-width 0.9 m + safety 0.1 m = 1.0 m; vehicle 4.5×1.8 m footprint checked as its medial spine sampled every 0.05 m against the inflated grid (stadium of radius 1.0 m ⊇ rectangle + exact 0.1 m lateral margin; longitudinal overhang ≈ 0.56 m per end = structural conservatism, identical across all cells so paired comparisons unaffected). Search budget 500k expansions / 90 s per problem; budget exhaustion reported as infeasible_budget SEPARATELY from provable 2D disconnection. Goal reached iff position within 0.30 m and heading within ±1 bin. Lattice snap error ≤ 0.071 m per step, absorbed by the 0.1 m safety inflation (documented approximation, same for all grids).
- **Tasks (paired):** 100 planning problems per scene, sampler seed 0; start/goal poses drawn ON the lattice, valid on the GT grid's inflated costmap, separation 5–25 m, point-robot-connected on the GT relaxed map. The SAME 100 problems are planned on every grid of the scene: toy {clean30k, B50 ft e1v2, B25 ft e1v2} × routes {i, ii} + GT reference; courtyard {clean30k, B50 ft e1v2} × routes {i, ii} + GT reference (12 planning cells, ~1200 plans, CPU only).
- **Grids REUSED, not recomputed (R3.a directive):** route-(i)/(ii) grids loaded from `/data/peilincai/gems_stage1/analysis/r3a_occupancy_routes/<cell>/grids_and_per_sample.npz`. GT occupancy is NOT stored in those npz; it is rebuilt via the identical `downstream_metrics` code path and VERIFIED bit-exact against the stored per-voxel indicators (free_at_gt_occ/occ_at_gt_free for both routes) and the stored seed-0 d2 GT verdicts before any planning. Any mismatch = abort.
- **Metrics per cell:** plans_found rate (spurious infeasibility = 1 − found, with sub-reasons start/goal-invalid, disconnected, budget); collisions-per-100-plans = exact 4.5×1.8 m footprint sweep of the PLANNED path (0.05 m pose/footprint sampling, d2 semantics) against the GT footprint layer; path-length inflation vs the GT-grid-planned reference on commonly-solved problems; wall-clock per plan. Paired bootstrap CIs (per-problem units) on the prediction comparisons.
- **PRE-REGISTERED PREDICTIONS (frozen now):** P1 (preservation, per the R-02 bit-identical-depth side finding): plans on route-(i) grids of B50 models produce ≤ the collisions-per-100-plans of the clean-model route-(i) grids on BOTH scenes (violated only if B50 − clean > 0 with paired CI excl. 0). P2 (the R-02 false-free deficit materializes as real collisions): route-(ii) grids produce MORE GT-collisions-per-100-plans than route-(i) grids per (scene × model), paired CI reported. Conservatism (path-length inflation + spurious-infeasibility) reported for BOTH routes either way — route (i) is the pre-registered SAFE consumer (R-02 verdict) and this cell is the downstream-application implementation; TSDF route (ii) was falsified in R-02 but is STILL run here as the comparison arm.
- Tool: `tools/gems/planner_loop.py` (consumes R3.a artifacts + `downstream_metrics` constants; no metric fork; numpy/scipy CPU). Durable outputs: `/data/peilincai/gems_stage1/analysis/r3c_planner/{summary.json, per-cell json, 3 example trajectory panels per scene}`. Iteration budget R3.c: 0/2 tuning variants consumed (all constants above frozen at pre-registration).
- **RESULT (2026-07-03): the closed loop ran end-to-end; the dominant finding is EXTREME CONSERVATISM, not collisions.** GT verification: 12/12 bit-exact checks (per-voxel indicators of every cell + seed-0 d2 GT verdicts) before any planning. Numbers (found/100 | coll-per-100-plans | mean len inflation vs GTREF | s/plan | lethal% after 1.0 m inflation): toy GTREF 100 | 1.0 | — | 0.92 | 11.4%; toy route-i clean/B50/B25 all 7 | 0.0 | +7.8% | 0.05 | 53.9%; toy route-ii all 10 | 0.0 | +5.3% | 0.07 | 45.9%; courtyard GTREF 100 | 2.0 | — | 0.34 | 68.5%; courtyard route-i clean/B50 0 | n/a | n/a | <0.01 | 84.2%; courtyard route-ii clean/B50 28 | 10.7 | +5.8% | 0.05 | 84.2%. Infeasibility is almost entirely start/goal-invalid (GT-valid poses sit inside the model grids' inflated false-occupied blankets), not search failure; 0 budget exhaustions.
- **P1 (preservation): PASS.** B50 planner outcomes are IDENTICAL to clean on every evaluable cell (toy route-i: same 7 plans, same paths, 0 collisions, paired diff CI [0,0]; route-ii grids bit-identical per R-02 → identical plans; courtyard both 0/100 found → trivially preserved, comparison unevaluable). B25 also identical-to-clean on toy (reported, not part of the bar). Compaction does not change what the planner does — preservation in the strongest observable form.
- **P2: FALSIFIED as pre-registered.** On toy the paired conditional comparison is evaluable and shows NO route-ii excess (0 vs 0 collisions on the 7 common-found problems; CI [0,0]) — both routes only plan in wide-open areas where either is safe. On courtyard it is UNEVALUABLE as frozen (route-i solves 0/100, so collisions-conditional-on-plan has no common support). In the ABSOLUTE sense the mechanism did appear: route-ii is the only route that plans on courtyard (28/100, found-diff CI [0.19,0.37] excl. 0) and those plans hit real GT geometry at 10.7 per 100 — 5.4× the planner's own GTREF floor (2.0; corner-clip of the zero-corner-margin stadium under ≤0.071 m lattice snap, floor reported honestly). The panel `courtyard__gt_collision.png` shows a route-ii plan sweeping through a GT wall its grid marks free.
- **Consumability verdict (the C4′-relevant sentence):** at 0.10 m voxels + the standard 1.0 m footprint inflation, RAW occupancy grids from BOTH routes are unusable for closed-loop footprint planning — route (i) because false-occupied floater clutter blankets free space (spurious infeasibility 93–100%), route (ii) because where it does plan on real scenes it collides (and it is bit-identical clean↔B50, so this is a clean-model geometry problem, not a compaction problem). Path-length inflation on solved problems is modest (+5–8%) — conservatism manifests as infeasibility, not detours. This quantitatively motivates R3.b (certified structural sub-mesh / floater removal BEFORE voxelization) as the missing consumability step; planner + harness are ready to re-consume R3.b grids unchanged.
- Caveats logged in summary.json: courtyard GT scan clutter in the vehicle band (44.5% of columns GT-occupied; frozen approximate z_band per GOAL #008) makes even GT free space peripheral; unscanned voxels count GT-free (collision counts a lower bound, affects all cells equally); out-of-ROI treated free (d2 semantics); GTREF floor 1–2/100 from corner clipping.
- Durable: `/data/peilincai/gems_stage1/analysis/r3c_planner/{summary.json, <cell>/cell_metrics.json × 12, panels/{toy_parking,courtyard}__{success,gt_collision,infeasible}.png}` (toy gt_collision panel = GTREF corner-clip example; no model-grid collision exists on toy). Wall-clock 201 s total, CPU only. Discipline: pre-registration frozen before any plan; 0/2 tuning variants consumed; selftest (analytic primitives, FFT-vs-brute-force collision maps, stadium coverage, plan/collision/disconnection cases) PASSED; grids reused bit-exact from R3.a; D4 green; no git commits. Insert R3.c is hereby closed with verdicts on toy + courtyard; SS3DM planner cells follow R3.e when B0s land.

### GOAL #R-04 — R2 execution: E2R-v1 opacity-floor release (probe + 2 FT runs + verdict) [R2] — 2026-07-03 — DONE (E2R-v1 FAILS the joint pre-registered bar: toy PSNR guard violated; courtyard is a REAL positive — best courtyard B50 row yet; variants 1/2 used)
- Pre-registration: GOAL #R-00 (frozen before code): o_min=0.01, λ_o=1e-4 (exponent adjustable only by the pre-run scale check), fade τ=0.05 / T=3 consecutive checks @500 iters, FT from B5@50 with channels {features, opacity}, positions frozen, 10k iters, ≤2 variants.
- **Implementation (surgical, default-off, no behavior change without the flag):** `--enable_learnable_opacity` releases the floor to `opacity_floor_min` at training_setup/resume and re-expresses loaded logits via `update_min_weight` (realized opacities preserved to float precision: max |Δrealized| 1.07e-6, max pixel Δ 9.5e-7 — NOT bit-for-bit, logit-space float32 roundtrip; documented deviation); vertex_weight group lr := `--opacity_lr` (0.05, logit-space Adam scale); `--lambda_opacity_decay` adds L_o = mean(realized opacity gathered per triangle-vertex — same form as the legacy Lweight, which is inactive past start_opacity_floor); the two legacy floor-re-pin schedule sites are guarded off under the flag; fade-and-prune (explicit override `--allow_fade_prune` on top of `--freeze_topology_updates`): every 500 iters mark triangles with MAX realized vertex opacity < 0.05, 3 consecutive marks → removal via existing `prune_triangles` keep-mask + unreferenced-vertex GC. Checkpoint format: `opacity_floor` persisted in the state dict; `load_parameters` reads it with default 0.999 (legacy ckpts bit-identical; eval mouth renders E2R models correctly — round-trip verified exact). Files: `arguments/__init__.py:121-150`, `scene/triangle_model.py:204-210,351-356`, `train.py:3511-3546,3630-3636,4160-4180,5289-5291,5335-5338,5350-5417,4683-4691`, `tools/gems/e2r_compositing_probe.py` (new), `tools/gems/e2r_verdict.py` (new). NO commits (working tree only).
- **Mandatory compositing-order probe (BEFORE runs; toy + courtyard B50 models, train views only, GPU 4): SANE.** Durable: `analysis/e2r_probe{,_courtyard}/probe_report.json` + panels. (i) determinism: repeat renders bit-identical; (ii) ε-order stability (1e-4 translation permutes center-depth sort keys at sub-pixel image shift): toy MAD 0.69/0.98/0.88e-4 (checkerboard) vs 0.29/0.47/0.72e-4 (opaque control), courtyard 6.3/2.8/4.7e-4 vs 5.3/1.6/3.0e-4 — cb/opaque ratio ≤2.4, no popping explosion; (iii) 0.2° perturbation SSIM: toy 0.92-0.95 (cb) vs 0.96-0.98 (opaque), courtyard 0.47-0.58 vs 0.69-0.72 — moderate multi-layer parallax, NO tile-shaped seams in panels; (iv) SINGLE triangle at o=0.01: max pixel Δ ≤ 6.2e-5 (vs visible opaque footprint ≤6e-3) → o_min triangles visually negligible; (v) AGGREGATE stacks of ~50+ coincident 0.01-α triangles accumulate to ≤0.54 max pixel Δ — correct alpha math (1−0.99^N), a property of the 62×-over-parameterized models, and exactly what fade-prune removes. λ_o scale sanity (pre-registered check): weighted L_o ≈ 1e-4 = 2.24% of toy photometric (0.00445) and 0.19% of courtyard photometric (0.0516) — sane regularizer scale, no >10× pathology → λ_o=1e-4 kept, exponent unchanged.
- **Runs (WANDB group gems_e2r, names gems_e2r_{toy_parking,courtyard}_B50; models `models/e2r_<scene>_B50_v1@50000`; both trains+evals EXITED:0):** fade-prune removed toy 131,124 tris (3.98% of B5's 3,295,279 → 3,164,155) and courtyard 44,650 (2.04% of 2,187,464 → 2,142,814); removal-only, budget ≤ B respected. Opacity distribution B5→E2R (vertex realized): toy mean 0.999→0.831, frac<0.05 0→12.3%, frac<0.5 0→17.2%; courtyard mean 0.999→0.881, frac<0.05 0→6.7%, frac<0.5 0→10.7% (median stays 0.999 — load-bearing content resists the decay, junk collapses to the floor: the intended differentiable trade DID operate).
- **Results vs B5 (paired bootstrap, PROTOCOL §5; evals `eval/e2r_<scene>_B50_v1`; courtyard B5 downstream produced via new same-mouth row `eval/courtyard_B50_importance_ft_e1v2_dsv1` — banked row untouched, PSNR/g1/g3 reproduce exactly):**
  - toy: ΔPSNR **−0.383 CI[−0.586,−0.201] → GUARD (≥−0.10) VIOLATED**; ΔLPIPS +0.0060 CI[+0.0043,+0.0079] (worse); g1 −1.3% rel (CI excl. 0, ≪30%); d1-ff +0.5% rel WORSE (CI excl. 0); g3 components 3362→1764 (**−47.5%**, ≥30% arm met; fraction −24.8%); g4/d2 unchanged.
  - courtyard: ΔPSNR **+0.460 CI[−0.151,+1.605]** (mean above B5; 5-view CI wide — CI-lo −0.15 does not certify the −0.10 floor at 95%); ΔLPIPS **−0.0063 CI[−0.0108,−0.0023]** (better, CI excl. 0); g1 0.0940→0.0672 (**−28.5%** rel, CI excl. 0 — just under the 30% bar); d1-ff +0.6% rel worse (CI excl. 0); g3 components 14134→9284 (**−34.3%**) / fraction 0.0251→0.0148 (**−41.1%**) — ≥30% arm met; panels show unambiguous visible cleanup (the black near-camera occluder blanket over DSC_0302's top-left corner is GONE; floater overlay 54,897→31,688 flagged tris).
- **VERDICT: E2R-v1 FAILS the joint pre-registered PASS bar** (requires toy AND courtyard): toy's PSNR guard is decisively violated while its only ≥30% arm (g3 components) lacks a CI by nature. Failure mechanism on toy = the E2v3 selection effect again, now via fading instead of deletion (panel test[7] 00035 gains pinholes; low-train-support content is what test-like views see) PLUS softened surfaces (17% of vertices below 0.5 opacity). Courtyard result stands as a REAL geometry+rendering improvement — the first mechanism in this project that removes junk WHILE IMPROVING rendering on a real scene (LPIPS CI excl. 0, PSNR +0.46 mean, g1 −28.5%, g3 −34/−41%) — recorded for C2′ consideration but NOT bar-clearing under D7 (one rule, all dev scenes).
- Surprises (recorded): (1) toy FPS 75.5→39.6 (semi-transparent stacks defeat early ray termination; courtyard only 122→117) — opacity release has a RUNTIME cost in this compositor; (2) d1-false-free moved slightly WRONG on both scenes (CI excl. 0) — removing/fading junk surfaces reduces accidental voxel coverage of GT-occupied space, consistent with R-02's coverage lesson; (3) fade steady-state not reached at 10k iters (toy still 12.3% of vertices <0.05 but unremoved — removal trickle ~2.3k tris/500 it at end).
- D4/D5 discipline: all numbers from run_eval.py; `audit_test_path` on the E2R courtyard row: **GREEN** (`eval/e2r_audit_courtyard/audit_report.json`); probe + evidence train-view-only. Iteration budget E2R: **1/2 variants used**. Sanctioned v2 (free-space hinge routed to opacity logits ONLY) remains available per R2; whether toy's guard failure makes v2 worth spending, and whether the courtyard-positive redeems a scene-scoped claim, is escalated to the R-loop coordinator/human — no self-amendment of the bar.

### GOAL #R-05 — S-GEO first Pareto slice (SS3DM 4 towns) — 2026-07-03 — DONE
- 4 B0 baselines trained (22.2–23.2 PSNR, 8.9–15.6M tris, 44–62 FPS) + B50/B25 × B5/B4 rows (20 evals, all jobs EXITED:0). **B50 B5 floor 3/4 towns (town01/02/03 positive mean dPSNR, LPIPS better 4/4); town06 fails (−0.447; least over-parameterized town). B25: 1/4.** C1′ transfers to the driving domain at B50. Gaps queued: g1 needs SS3DM GT-depth branch (converter wrote empty POINTS2D → colmap-sparse g1 inactive); g4 needs GT-mesh alignment (cm→m + left-hand mirror) + ROI freeze + Town06 RAM strategy.

### GOAL #R-06 — R3.b certified structural sub-mesh: one-time global triangle labeling → collision-grade sub-mesh → route-i → d1/d2 + planner [R3.b, elevated by R-03's consumability verdict] — 2026-07-03 — DONE (PREDICTION FALSIFIED on all 4 bar cells: certification moves route-i onto route-ii's false-free failure branch; POSITIVE side finding: the certified artifact is EXACTLY invariant clean↔B50 on both scenes)
- **Why (from R-02/R-03):** raw route-i grids cause 93–100% spurious planner infeasibility (false-occupied floater/junk blankets swallow GT-valid poses); route-ii is falsified (false-free → real collisions on courtyard). R3.b tests whether a certified structural sub-mesh restores closed-loop consumability. Framing guard (insert §R3): this is a ONE-TIME, GLOBAL, TRAIN-EVIDENCE-ONLY artifact (D4) — one keep-set per checkpoint, frozen as npz; nothing varies per view or per query at consumption. It is an artifact-generation step, NOT a selector.
- **PRE-REGISTERED MECHANISM (frozen now):** per checkpoint, over the SAME ≤60 evenly spaced train views as the g3 support pass (`np.unique(np.linspace(0, n_train−1, min(60,n_train)).round())`), training-time render settings via eval_context:
  - support_v(t) = triangle t owns ≥1 valid pixel in rend_ids gated by depth_full>0 (the exact g3 `_support_counts` definition);
  - consistent_v(t) = support_v(t) AND t's center (mean of its 3 vertices) projects in-frame & in-front (repo-exact `_project_points`) AND rend_alpha(px,py) ≥ 0.5 AND surf_depth d_r(px,py) finite>0 AND |depth(center) − d_r| ≤ τ_d·d_r;
  - global counts support(t)=Σ_v support_v, cons(t)=Σ_v consistent_v; **keep(t) = finite(t) AND support(t) ≥ k AND cons(t) ≥ m.** Kept set = "collision-grade sub-mesh", consumed by route-i voxelization (the exact d1 `_rasterize_triangles`) → d1/d2 → the R-03 planner harness UNCHANGED.
  - **τ_d FROZEN = 0.10** (justification, before any number: two-sided analog of the g1 free-space ratio band — g1 flags at 5% one-sided; doubling tolerates the finite thickness of stacked near-duplicate surfaces in these 62×-over-parameterized models without admitting gross floaters).
- **Sanctioned ONE-TIME calibration (≤3×2, toy clean30k ONLY, table logged):** k ∈ {2,3,5} × m ∈ {1,2}. Objective: minimize planner spurious-infeasibility s.t. (c1) collisions-per-100-plans ≤ toy GTREF floor + 1 = 2.0 (a combo with 0 plans found satisfies c1 vacuously — it loses on the objective anyway) and (c2) d1 false-occupied ≤ 1.5× raw route-i (structurally guaranteed: kept faces ⊆ all faces ⇒ sub-mesh occupancy ⊆ raw occupancy ⇒ fo_sub ≤ fo_raw; measured and reported anyway). Tie-break: lower collisions-per-100, then lower d1 false-free, then grid order. FREEZE (k*,m*); apply UNCHANGED to toy{clean30k, B50 ft e1v2} and courtyard{clean30k, B50 ft e1v2}; toy B25 reported as an extra frozen-params row (not part of the bar), mirroring R-03's B25 handling.
- **PREDICTION (frozen):** the certified sub-mesh restores planner feasibility — found-rate ≥ 80/100 on both toy bar cells and ≥ 30/100 on both courtyard bar cells — at collisions-per-100-plans ≤ scene GTREF floor + 1 (toy ≤ 2.0, courtyard ≤ 3.0), with d1 false-free not worse than raw route-i by more than 10% relative per bar cell. PASS iff all three arms hold on all 4 bar cells.
- **Harness reuse + verification (abort on any mismatch, before any sub-mesh planning):** planner problems = the identical seed-0 100 problems per scene (deterministic re-derivation from the rebuilt GT grid); (v1) rebuilt GT occupancy reproduces the stored R3.a per-voxel indicators + stored seed-0 d2 GT verdicts bit-exact (verify_gt_grid, as R-03); (v2) re-planned GTREF per-problem records match the stored R-03 GTREF records exactly on (found, reason, n_expansions, path_length_m, n_segments, n_switches); (v3) voxelizing keep=ALL-finite-faces reproduces the stored occ_route_i bit-exact per cell. Paired comparisons: vs stored R-03 raw route-i per-problem records (found-rate diff CI, collision diff CI on common-found) and path-length inflation vs GTREF on common-found; d1 ff paired-bootstrap CI (units = GT-occupied voxels) vs raw route-i.
- Tool: `tools/gems/certified_submesh.py` (reuses geometry_metrics support/projection, downstream_metrics grids, occupancy_routes GT adapter, planner_loop planner — no metric fork). Durable: `/data/peilincai/gems_stage1/analysis/r3b_submesh/{labels npz per cell, calibration_table.json, frozen_params.json, summary.json, per-cell json, panels (2 per scene: before/after occupancy footprint + one planned path)}`. One GPU for the labeling render passes only; planner CPU. Iteration budget R3.b: the k×m grid is the sanctioned one-time calibration; 0/2 tuning variants consumed. D4: train views only; toy GT enters ONLY through the sanctioned calibrate-once objective on toy clean. No git commits.
- **RESULT (2026-07-03): FALSIFIED — 0/4 bar cells pass the joint prediction.** Verification all green before any sub-mesh number: v1 GT bit-exact (toy 7 + courtyard 5 checks), v2 GTREF replans identical to R-03 per-problem (floors 1.0 / 2.0 per 100), v3 all-finite-faces voxelization bit-exact vs stored occ_route_i on all 5 cells → grids and 100 seed-0 problems fully paired with R-03.
- **Calibration (toy clean30k, all 6 combos logged, ALL feasible — the two constraints never bound: coll/100 = 0.0 everywhere, fo ≤ raw by the subset property):** found-rate is monotone in removal aggressiveness — k2m1 kept 24.8% found 8/100 → k3m1 18.3% 10/100 → k5m1 11.4% **14/100** (best); ff climbs 0.6288 → 0.6855 in step. **FROZEN k=5, m=1** (m by the ff tie-break, 0.6855 < 0.6865). Decisive early warning honestly carried forward: even the best combo reaches only 14/100 vs the 80/100 bar ON THE CALIBRATION CELL ITSELF.
- **Verdict table (found/100 | coll-per-100 (cap) | d1 ff sub vs raw (rel Δ) | d1 fo sub vs raw | kept tris (frac of finite) | cell verdict):**
  - toy clean30k: 14 ✗(≥80) | 0.0 (≤2.0) ✓ | 0.6855 vs 0.5895 (+16.3%) ✗ | 0.0198 vs 0.0305 | 749,367 (11.4%) | **FAIL**
  - toy B50: 14 ✗ | 0.0 ✓ | 0.6855 vs 0.5974 (+14.7%) ✗ | 0.0198 vs 0.0296 | 749,367 (22.7%) | **FAIL**
  - courtyard clean30k: 42 ✓(≥30) | 16.7 (≤3.0) ✗ | 0.8489 vs 0.6503 (+30.5%) ✗ | 0.0307 vs 0.1053 | 340,265 (7.8%) | **FAIL**
  - courtyard B50: 42 ✓ | 16.7 ✗ | 0.8489 vs 0.6680 (+27.1%) ✗ | 0.0307 vs 0.0958 | 340,265 (15.6%) | **FAIL**
  - toy B25 (extra, not bar): 14 | 0.0 | 0.6822 (+10.9%) | 0.0201 | 752,381 (45.7%)
  - All ff paired CIs exclude 0 on the worse side (toy clean +0.0960 [0.0939,0.0981]; courtyard clean +0.1986 [0.1956,0.2016]; units GT-occupied voxels). Found-rate gains vs raw route-i are REAL (toy +0.07 CI [0.03,0.12]; courtyard +0.42 CI [0.32,0.52]) — but courtyard's unblocked plans hit real GT geometry at 16.7/100, WORSE than route-ii's 10.7 (GTREF floor 2.0), and courtyard d2 unsafe_disagreement moves 0.0 → 0.064 (first nonzero unsafe d2 in the route-i family). Path-length inflation on solved problems stays modest (+4.7% toy / +6.4% courtyard mean).
- **Mechanism (reported, one sentence per scene):** on toy, removing 88.6% of triangles only cuts lethal 53.9%→48.0% (GT 11.4%) and 85/86 not-found remain start/goal-invalid — the residual blanket is ≥5-view-supported, depth-consistent structure the model itself renders, so support+consistency cannot separate it from GT-band truth; on courtyard, certification cuts lethal 84.2%→62.8%, BELOW the GT 68.5%, i.e. it sheds real walls together with junk (ff 0.65→0.85; the footprint panel shows large GT regions with zero model coverage) — multi-view-supported junk and load-bearing surface are not separable by this evidence, the same selection-effect family as E2v3/E2R-toy. Toy calibration could not expose this risk (its certified grid stays collision-free), so calibrate-once/test-elsewhere transferred the aggressive threshold honestly and it failed on the real scene.
- **POSITIVE side finding (strongest preservation statement in the project so far):** the certified kept SET is EXACTLY identical clean↔B50 on BOTH scenes — identical counts (toy 749,367; courtyard 340,265) AND float-exact identical kept-triangle centroid sets (verified directly against both checkpoints) → labels, sub-mesh occupancy grids (bit-identical), d1/d2, and all 100 planner outcomes are invariant to B50 compaction. Extends the R-02 bit-identical-depth finding from rendered depth to a full train-evidence certification artifact. toy B25 differs only microscopically (+3,014 kept; ff 0.6822 vs 0.6855) and still yields identical planner outcomes.
- **Consumability conclusion (C4′-relevant):** with R-02 (TSDF route falsified), R-03 (raw grids unusable), and R-06 (certification trades spurious infeasibility for real collisions), every one-time consumption route tested at 0.10 m voxels + 1.0 m inflation fails parking-grade closed-loop planning; the blocker is the checkpoint geometry itself — supported-and-consistent structure is wrong or missing in the vehicle band — not the consumption method. This strengthens C2 (geometry unreliability is mechanism-deep) and quantifies the gap a geometry-level fix must close on courtyard: ≥30/100 found at ≤3.0 coll/100 simultaneously (best achieved: 42/100 at 16.7).
- Durable: `/data/peilincai/gems_stage1/analysis/r3b_submesh/{summary.json, calibration_table.json, frozen_params.json, <cell>/{labels.npz, submesh_grids.npz, cell_metrics.json} ×5, panels/{toy_parking,courtyard}__{footprint_before_after,submesh_planned_path}.png}`. Wall-clock 438 s total (labeling 10–20 s/cell on GPU 1, rest CPU). Engineering note: first run crashed at the final summary.json write (non-serializable planner-map object placed in the summary dict); fixed, full re-run reproduced every number deterministically.
- Discipline: pre-registration frozen before any label existed; sanctioned one-time calibration only, 0/2 tuning variants consumed (no post-hoc threshold edits — the courtyard failure was NOT answered with a re-calibration); selftest PASSED (keep-mask semantics, occupancy subset property, calibration tie-breaks, v2 mismatch detection); D4 green; framing guard held (no per-view/per-query logic anywhere; artifact-generation, not selection); no git commits. Honest negative closure per insert R3.b.
- Caveats: absolute k,m calibrated at 60 label views (toy) but applied at 33 (courtyard) — a support-fraction variant is NOT sanctioned and was not run; depth consistency uses the triangle CENTER only (large rim-load-bearing triangles can be over-removed); courtyard GT-scan caveats from R-03 (unscanned voxels GT-free → collision counts a lower bound; approximate z_band) carry over unchanged; toy GTREF floor 1.0 / courtyard 2.0 per 100 is the planner's own corner-clip floor.

### GOAL #R-07 — R4 pristine-build check + STAGE 1R COMPLETION — 2026-07-03 — DONE
- Pristine-submodule build check: **PASS (Outcome A)** — zero source drift vs pinned b27f283 (dirty flag = stale committed pyc, forensically inverted); fresh-clone build reproduces toy clean30k eval **bit-for-bit** (18 views × PSNR/SSIM/LPIPS float64-hex identical) AND matches the banked row. Landmine cleared; residual hazard documented (tracked stale prebuilt _C.so binds on in-tree cwd imports — no repo script does this). Artifacts: analysis/r4_pristine_build/.
- **STAGE 1R COMPLETE per the insert's criteria**: R1 ✓ (primary anchor, 9/9; CLAIMS v1.1) · E2R ✓ (joint-bar tombstone + bounded courtyard positive → B6R) · R3.a/R3.c ✓ (+ elevated R3.b: consumption trilogy closed with quantified fix-target) · SS3DM ✓ INTEGRATED (D-1/D-1b + first Pareto slice) · R4 ✓ (MATRIX amendments, CLAIMS v1.2, RED-TEAM section, pristine build, Delta Memo #001). Honest-negative closures per the insert's final paragraph: R1-T positive; E2R negative-with-bounded-positive; R3.a negative. Stage 2 resumes under amended CLAIMS/MATRIX (B12.5 batch in flight).

### GOAL #010 — B12.5 slice (S-REND) — 2026-07-03 — DONE
- 18 rows (9 scenes × B5/B4 @12.5%), all jobs EXITED:0. **0/9 within −0.10 of clean (−0.31..−2.37 dB; graceful degradation, FPS 48–76) — iso claim consolidates at B50. FT value 9/9 positive CI, growing as budget shrinks (+0.09..+0.27), confirming the E1′/G4 regime prediction.** Next: random contrast at B12.5 (the A2 dominance measurement).

### GOAL #011 — B12.5 dominance contrast — 2026-07-03 — DONE
- 9 random_ft@B12.5 rows (safe-FT matched config), all EXITED:0. **Evidence-importance dominates random by +3.63..+8.19 dB (mean +5.23), 9/9 CIs excl. 0** — the E1'/G4 regime claim is fully evidenced; RED-TEAM A2 answered with measured margins. E1-PARETO S-REND core structure complete: B50 iso (8/9 legacy / 4/9 primary anchor), B25/B12.5 graceful degradation with FT value growing (9/9 CI), aggressive-budget dominance quantified.

### GOAL #R-09 — E9-FAIL failure taxonomy dossier [Matrix: E9-FAIL] — 2026-07-03 — DONE
- Infra goal (no hypothesis; CPU-only curation of banked artifacts). Built `/data/peilincai/gems_stage1/analysis/e9_failure_taxonomy/`: **13 curated failure cases, 24 panel PNGs copied from banked eval/analysis rows + 1 generated** (case11 R3.a d1 z-collapse viz from the archived `r3a_occupancy_routes` npz — no recomputation), each case with a one-paragraph mechanism diagnosis, evidence pointers (LEDGER GOAL + eval row), and a "what it bounds" line, in `TAXONOMY.md`.
- **Taxonomy = 5 mechanism families**: A train-coverage selection effects (cases: toy B50 residual, E2v3 00035 −8 dB, E2R toy fading, R3.b wall-shedding); B optimizer drift on converged checkpoints (default-FT garden −2.9, E2 gradient-routing); C budget starvation/headroom exhaustion (flowers B50/B25, town06 B50, courtyard random-B25 −3.97, B12.5 indoor kitchen −2.37); D view-conditioning capacity mismatch (E3v2 SH overfit LPIPS 0.11→0.21); E baseline-geometry unreliability & consumption-route bias (R3.a TSDF grazing-ray, garden g3 fragmentation 1941→5724 at iso-PSNR). One design implication per family recorded.
- Verification discipline: every referenced panel confirmed on disk; 11 panels visually inspected against their claims before inclusion; all quoted numbers re-derived from banked per-view `metrics.json` arrays and matched LEDGER values. **Candidates rejected: 0**; two retained with explicit qualifications (flowers = metric-level failure, panel evidence is diffuse error-map speckle only; garden fragmentation overlays subtle at panel scale — counts carry the case). MATRIX E9-FAIL: TODO → DONE (≥10 cases + diagnoses delivered). No git commits; no GPU use.

### GOAL #R-08 — S-GEO Tier-1 activation: SS3DM g1/g2 GT-depth branch + g4/d1/d2 GT-mesh alignment + ROI freeze + re-evals — 2026-07-03 — DONE (pre-registered preservation CONFIRMED on all 4 towns, 20/20 arms; g3 fragmentation surprise recorded)
- **Why (R-05 gaps):** g1 was inactive on SS3DM (converter writes empty POINTS2D → the colmap-sparse branch finds no visible points); g2 inactive (no gt_depth_dir); g4 ran but was GARBAGE (raw-cm, unmirrored mesh: chamfer 11k–77k m) — those R-05 g4 cells are hereby VOID; d1/d2 gated on the unfrozen ROI.
- **Implementation (all frozen in `tools/gems/scenes.py`, derivations in `/data/peilincai/gems_stage1/analysis/sgeo_roi_freeze/`):**
  - g1/g2: `tools/gems_train/ss3dm_export_gt_depth.py` exports per-TEST-VIEW float32 z-depth .npy at the -r 2 render resolution (960×540): decode raw_uint16 × 1000/65535 m (manifest-verified scale), sky/far-plane saturation raw ≥ 65534 → invalid(0), nearest at target pixel centers d[1::2,1::2]; 57 views/town. VERIFIED: npy vs LiDAR projection median ratio 0.9975 (94% within 5%); B0 rendered-median-depth/GT median ratio ≈ 0.99 in the 10–20 m band — the overall per-view median ≈ 0.86 is REAL model error (near-field junk <5 m ratio 0.82; far-field collapse: 40–80 m ratio 0.38, >150 m 0.08 — the model has no reliable geometry beyond ~40 m), which is exactly what g1/g2 now measure. NOTE: SS3DM GT depth reaches ~1000 m (sky excluded), so g1 samples include far-field pixels toy never had — g1 absolute values are NOT comparable across datasets.
  - g4/d1/d2: new `gt.mesh_transform` (4×4 on OBJ vertices after load; scan_transforms-sibling semantics, also honored by the downstream GT-occupancy path via run_eval pass-through) = diag(0.01,−0.01,0.01) (cm→m THEN diag(1,−1,1) world mirror; diagonal ⇒ commute). VERIFIED per town: LiDAR→transformed-mesh median NN 0.073/0.103/0.076 m (towns 01/02/03) vs unmirrored control 33.0 m / no-overlap / 0.13 m (town03 control weak — symmetric road grid; disambiguated by:) prescribed projection check = per-bin min-depth mesh samples vs raw GT depth PNG median ratio 1.0028/1.0045/1.0130.
  - New `gt.mesh_roi_presample_crop` (SS3DM-only; toy rows bit-exact untouched): town meshes are ~4–11 km² incl. far terrain vs ~0.03 km² ROI → the frozen 1M-on-full-mesh g4 sampling would leave ~2.8k/55k/7.1k in-ROI GT points (NN spacing up to ~3 m ≫ τ=0.05 m; recon→gt would measure GT sampling sparsity, not geometry). Flag restricts GT sampling to ROI-intersecting faces, defined at this FIRST valid SS3DM g4 eval (no prior non-void row). CAVEAT (both models equally): even cropped, 1M GT samples over ~3×10⁴ m² → NN spacing ~0.09 m ≈ 2τ — absolute F@5cm/chamfer are density-limited; paired deltas are valid.
  - ROI freeze (per town): xy = camera-centers xy-bbox ± 30 m; up-axis verified +z in the mirrored frame (mean camera up ≈ [0,0,+0.99], min dot 0.966 — courtyard-M5 pattern); ground = P1 of LiDAR z within camera xy-footprint+1 m = −0.069/0.150/−0.073/−0.071 m (cameras 2.25–2.47 m above — vehicle roof; flat: per-trajectory-fifth ground spread ≤ 7 mm); z_band = [ground, ground+1.5]; ROI z = [ground−0.3, P99.9(in-ROI LiDAR z)+0.3]. d1 grids ≈ 10.3M voxels/town at 0.10 m.
  - **Town06 RAM ruling:** measured trimesh load (exact g4 path, process=False) peak RSS 24.7 GB (13.6M faces, Scene→concat) > the pre-registered 20 GB bar → town06 g4/d1/d2 **INFEASIBLE-for-now** (mesh_path=None + note; NOT silently decimated; g1/g2/g3 active). Towns 01/02/03: 13.3/5.1/11.1 GB — feasible. (The R-05 town06 g4 garbage row had loaded it once at 24.7 GB; the bar rules regardless.)
- **PRE-REGISTERED EXPECTATION (frozen NOW, before any of the 8 evals):** per the preservation-exactness precedent (R-02 bit-identical rendered depth clean↔B50; R-06 float-exact invariant certification artifact; R-03 P1), **B50 (B5 = importance_ft, tag s2) preserves g/d metrics within noise per town**: paired 95% CIs (PROTOCOL §5) of clean-vs-B50 deltas on g1, g2, g4 (gt→recon), d1 ff/fo, d2 agreement include 0 OR the relative effect is below the 20% D3 geometry floor. g3 is NOT part of the preservation claim (pruning removes floater components by construction — expected to improve). g1/g2 clean ABSOLUTE values are unknown — measurement, not prediction. Evals: 4 towns × {clean30k, B50 B5} → `eval/<scene>_{clean30k,B50}_geo_v1`, GPUs 1–2, geometry+downstream ON.
- **RESULT (2026-07-03): all 8 evals EXITED:0 (5–8 min each; every metric active as designed). PRE-REGISTERED PRESERVATION CONFIRMED: 20/20 CI'd arms preserved** (towns 01/02/03: g1, g2, g4 gt→recon, d1 ff, d1 fo, d2 agreement; town06: g1, g2). Max wrong-direction relative effect = d1 ff +2.3% (≪ 20% floor). Full table + paired CIs: `analysis/r08_sgeo/{r08_table.md, r08_summary.json}` (builder `build_table.py`; sample-alignment asserted per metric before pairing — g1 pixel sets and d2 GT verdicts identical clean↔B50).
- **S-GEO table (clean → B50 per town; Δ = B50−clean, 95% paired CI):**
  - town01 (12.05M→6.03M tris, PSNR 22.57→22.63, FPS 53→70): g1 0.6418→0.6412 (Δ −0.00067 CI[−0.00072,−0.00061]); g2 6.980→6.987 m; g3 comps 3,842→10,395 / frac 0.0048→0.0122; g4 chamfer 1.624→1.648 m (gt→recon +0.032 CI excl. 0, +1.2%), F@5cm 0.0445→0.0446; d1 ff 0.8135→0.8318 (+2.3% rel, CI excl. 0) / fo 0.0674→0.0608 (−9.8%, CI excl. 0); d2 agree 0.690→0.685 (CI incl. 0), unsafe 0.292→0.297 (n_gt_coll 195).
  - town02 (15.57M→7.79M, 22.62→22.67, 48→64): g1 0.6022→0.6020; g2 3.937→3.938 m; g3 3,686→7,173 / 0.0029→0.0054; g4 2.685→2.684 m (gt→recon −0.0066, CI excl. 0 BETTER), F 0.0306→0.0310; d1 ff 0.7733→0.7892 (+2.1%) / fo 0.0655→0.0604 (−7.8%); d2 agree 0.690→0.690 (verdicts IDENTICAL, unsafe 0.3005 both).
  - town03 (15.15M→7.57M, 23.18→23.28, 50→65): g1 0.5466→0.5461; g2 5.080→5.080 m (CI incl. 0); g3 3,470→8,026 / 0.0035→0.0072; g4 1.989→2.009 m (gt→recon +0.0084, +0.2%), F 0.0501→0.0515; d1 ff 0.8047→0.8232 (+2.3%) / fo 0.0743→0.0661 (−11.0%); d2 agree 0.640→0.635 (CI incl. 0), unsafe 0.229→0.235 (n_gt_coll 153).
  - town06 (8.92M→4.46M, 22.21→21.77, 64→80; g4/d1/d2 INFEASIBLE per the RAM ruling): g1 0.7479→0.7446 (Δ −0.0033, CI excl. 0 — B50 slightly BETTER); g2 16.71→16.75 m (+0.2%); g3 4,723→14,694 / 0.0098→0.0299.
- **Measurements (first valid S-GEO absolutes; C2 transfers to the driving domain):** g1 free-space violation 0.55–0.75 — dominated by far-field hallucinated shells (GT depth reaches ~1000 m; model collapses beyond ~40 m: rendered/GT median 0.38 at 40–80 m, 0.08 at >150 m) plus near-camera junk (<5 m ratio 0.82); NOT comparable to courtyard's colmap-pair g1 0.09. g2 L1 3.9–16.7 m (town06 = highway, most far-field). g4 chamfer 1.6–2.7 m, F@5cm 0.03–0.05 (recall-limited: GT in the 30 m ROI margin includes street-invisible surfaces the model never saw; plus the ~0.09 m GT sampling floor). d1 ff 0.77–0.81 (vs courtyard 0.65) / fo 0.061–0.074; d2 agreement 0.64–0.69 with unsafe 0.23–0.30 (GT collision rate 77–98% — ROI is dense city). Parking/driving-grade consumption bar from R-06 remains far out of reach on SS3DM too.
- **Surprises:** (1) **g3 fragmentation, all 4 towns**: floater components UP 1.9–3.1× after B50 pruning (opposite of the naive expectation noted at pre-registration; g3 was excluded from the bar) — importance-pruning splits connected components; same family-E mechanism as garden 1941→5724 (E9 taxonomy case). (2) d1's paired signs replicate E2R/R-02's coverage lesson on every mesh town with tight CIs: ff slightly WORSE (+1.6..+1.8 pp — pruned junk was accidentally covering GT-occupied voxels), fo BETTER (−0.5..−0.8 pp). (3) g1 moved slightly but CI-significantly BETTER everywhere (−0.0002..−0.0033) — junk removal reduces free-space violations; largest on town06, the town whose PSNR fails the B50 floor. (4) d2 is preservation-exact in the strongest sense on town02 (identical verdict vectors) and within 1 trajectory/100 elsewhere.
- Durable: `eval/ss3dm_town0{1,2,3,6}_{clean30k,B50}_geo_v1/` (8 rows), `analysis/sgeo_roi_freeze/` (derivation script + 4 per-town jsons + stage-B logs), `analysis/r08_sgeo/`, `datasets/ss3dm_town0*/gt/depth/` (4×57 npy + export manifests). Discipline: pre-registration frozen before any eval; single mouth (run_eval.py) for all numbers; paired bootstrap = tools/gems/paired_bootstrap.py, seed 0, 10k resamples; toy/courtyard code paths untouched (mesh_transform/crop are opt-in per-scene gt keys; legacy rows bit-exact); no git commits; GPUs 1–2 only.

### GOAL #012 — E6-ABL importance-family ablation @B50 [Matrix: E6-ABL] — 2026-07-03 — DONE (revision trigger NOT tripped: importance v1 = pixels_total stands; family choice is near-irrelevant at B50 under safe FT)
- **PRE-REGISTERED (frozen before any new prune/FT/eval runs):** ablate the importance DEFINITION inside the B5 recipe at B50 on the declared E6 subset {garden, kitchen (S-REND), ss3dm_town01 (S-GEO)}. Families = evidence-npz columns (all from the SAME cached per-ckpt evidence npz, one render pass, D4 train-only): (a) `pixels_total` (importance v1, GOAL #005 pre-registration) — rows EXIST, reused as-is: `eval/garden_B50_importance_ft_e1v2`, `eval/kitchen_B50_importance_ft_s2`, `eval/ss3dm_town01_B50_importance_ft_s2`; (b) `max_blending_max` (per-triangle max over train views of the renderer's max_blending output); (c) `ckpt_importance_score` (training-time accumulated stat read from the raw source state dict). Identical ranking rule for all families: descending stable argsort, tie-break by triangle id, top-keep_count.
- **Implementation:** `--importance-key {pixels_total|max_blending_max|ckpt_importance_score}` added to `tools/gems/gems_pipeline.py` (default pixels_total = historical behavior). VERIFIED before launch: default-key prune cfg string rebuilds the banked kitchen s2 prune stamp hash EXACTLY (7fda0acf…, no stamp invalidation); non-default keys produce distinct prune-stage hashes via the importance token in the cfg string → prune+FT+eval re-run, evidence stage cache-hits. `importance_key` recorded in row.json.
- **6 new runs** (2 families × 3 scenes), config identical to the banked family-(a) rows: mode importance_ft, budget 0.5, --ft-iters 10000 --ft-position-lr 0.0 --ft-weight-lr 0.0 (features-only safe FT), seed 0, tags `abl_blend` / `abl_ckptimp`; same source ckpts as the banked rows (garden/kitchen official_clean30k@30000; town01 gems B0@30000 — fingerprints match the cached evidence npz names). GPU 1 = abl_blend chain, GPU 2 = abl_ckptimp chain (sequential per GPU, supervised jobs e6abl_gpu1/e6abl_gpu2); GOAL #013 holds GPUs 3/5 — no overlap.
- **PREDICTION:** pixels_total ≥ both alternatives on PSNR per scene (it is the fresh render-contribution signal the recipe was designed around; ckpt_importance_score is a stale training-window stat; max_blending saturates at the 0.999 opacity ceiling → coarse ties). REVISION TRIGGER: any family beating pixels_total by ≥0.10 dB with paired CI excl. 0 on ≥2/3 scenes → recipe-revision candidate (re-opens the importance definition, human-gated). Expected magnitudes small (keep-set overlap vs (a) at B50, computed pre-launch: garden 94.8%/94.7%, kitchen 96.9%/96.3%, town01 100%/74.1% for (b)/(c)).
- **Pre-launch degeneracy finding (recorded BEFORE runs):** on ss3dm_town01, family (b)'s keep SET is EXACTLY identical to (a)'s (verified np.array_equal): only 32.4% of triangles have nonzero evidence (< the 50% budget), and both columns share the same nonzero support + id-ordered zero tail. The town01 abl_blend run therefore re-runs an identical prune and measures pure FT+eval reproducibility noise (prediction: |Δ| ≈ 0, a free noise-floor row); the family-(b) verdict rests on garden+kitchen. Columns otherwise non-degenerate (all finite; nonzero fractions 32–68%).
- **Analysis plan:** 3×3 table (scene × family): PSNR/LPIPS vs the scene's clean row (`garden_clean30k_v2`, `kitchen_clean30k_v1`, `ss3dm_town01_clean30k_v1`; legacy clean@30k anchor for uniformity across S-REND/S-GEO — cleanfixed does not exist for town01; dual-anchor caveat noted) + paired family-vs-family CIs (b−a, c−a, c−b) on per-view PSNR/LPIPS via `tools/gems/paired_bootstrap.py` (seed 0, 10k resamples), image-name alignment asserted. Builder + table → `analysis/e6_abl/`. Evals via the pipeline's own eval stage (run_eval.py, D5). No git commits.
- **RESULT (2026-07-03): all 6 runs EXITED:0 under supervision (chains e6abl_gpu1/gpu2); table + CIs in `analysis/e6_abl/{e6_table.md,e6_summary.json}`, evals `eval/{garden,kitchen,ss3dm_town01}_B50_importance_ft_{abl_blend,abl_ckptimp}`.**
- **3×3 table (PSNR | dPSNR vs clean | LPIPS; `*` = paired CI excl. 0):**
  - garden (clean 24.712/0.2163): (a) 24.851 +0.139* 0.2101 | (b) 24.853 +0.141* 0.2102 | (c) 24.849 +0.137* 0.2103. Pairwise dPSNR: b−a +0.003 [+0.000,+0.005]*, c−a −0.002 [−0.006,+0.001], c−b −0.005 [−0.010,−0.001]*.
  - kitchen (clean 27.296/0.2262): (a) 27.449 +0.154* 0.2191 | (b) 27.449 +0.154* 0.2190 | (c) 27.437 +0.141* 0.2193. Pairwise dPSNR: b−a −0.000 [−0.006,+0.004], c−a −0.012 [−0.021,−0.004]*, c−b −0.012 [−0.021,−0.005]*.
  - ss3dm_town01 (clean 22.574/0.2831): (a) 22.634 +0.061 0.2798 | (b) 22.634 +0.061 0.2798 | (c) 22.582 +0.009 0.2798. Pairwise dPSNR: b−a −0.00002 [−0.00004,+0.0000] (the pre-registered degenerate-identical-prune row), c−a −0.052 [−0.121,−0.001]*, c−b −0.052 [−0.121,−0.001]*. dLPIPS vs clean −0.003* all families, pairwise dLPIPS |Δ| ≤ 0.0002 everywhere.
- **VERDICT: REVISION TRIGGER NOT TRIPPED (0 scenes for either family vs the required ≥2 at ≥+0.10 dB CI excl. 0) → importance v1 = pixels_total remains the recipe.** Prediction letter ("a ≥ b,c on PSNR per scene") technically falsified on garden by b−a = +0.0025 dB CI[+0.0005,+0.0051] — a micro-delta 40× below the D3 floor and 40× below the trigger; substantively the prediction held (c ≤ a on 3/3, b ≈ a). Largest family effect anywhere: c−a town01 −0.052 dB.
- **Surprises:** (1) **The importance-family axis is nearly flat at B50 under safe FT** — all pairwise |dPSNR| ≤ 0.052 dB (cf. importance-vs-random +3.6..+8.2 dB at B12.5, GOAL #011): with 94–97% keep-set overlap and features-only FT repainting survivors, WHICH render-evidence column ranks triangles barely matters; what matters is evidence vs none. (2) **Measured pipeline noise floor 1.6e-5 dB**: town01 (b) re-ran prune (bit-identical keep set) + seeded 10k FT + eval and reproduced (a)'s PSNR to |Δ| = 0.00002 dB [CI ±0.00004] — the seeded FT+eval path is reproducible at the 1e-5 dB scale, so 0.01-dB-scale paired family deltas are real effects, not run noise. (3) `ckpt_importance_score` is the consistently (slightly) WORST family — c ≤ a on 3/3 scenes, CI excl. 0 on 2/3 (kitchen −0.012, town01 −0.052) — direction supports the "fresh render-contribution beats stale training-window stat" rationale, magnitude far below D3. (4) FPS/tris identical across families (same keep_count by construction).
- Discipline: pre-registration + degeneracy finding frozen before launch; same-mouth evals; paired bootstrap seed 0/10k; banked family-(a) rows reused untouched; default-key stage hashes verified unchanged (kitchen s2 prune stamp rebuilt exactly); no git commits; GPUs 1–2 only, kill-by-pidfile never needed.

### GOAL #013 — B3 QEM baseline (B50 × 3 dev scenes) + H1/R1 disposition [Matrix: E1-PARETO B3 column, H1, R1] — 2026-07-03 — IN PROGRESS
- **PRE-REGISTERED (frozen before any decimation/FT/eval runs):** B3 = importance-free geometric decimation (quadric-edge-collapse, open3d 0.19 `simplify_quadric_decimation` — availability verified in-env; trimesh present as fallback, pymeshlab absent) to keep_count = floor(0.5·T_clean) faces (same keep_count as the banked B5/B4 rows: garden 5,784,028; toy 3,295,279; courtyard 2,187,464; fair-budget ±2% enforced by assertion), per-vertex attributes (vertex_weight, features_dc, features_rest) transferred by nearest-ORIGINAL-vertex (cKDTree on finite original vertices); non-finite faces excluded from decimation input (toy has 13 NaN faces); checkpoint rebuilt to the exact point_cloud_state_dict.pt contract (mirrors checkpoint_compaction: metadata copy + topology audit + load/render-1-train-view verification); then the validated safe FT (features-only: --weight_lr 0.0 --lr_triangles_points_init 0.0, 10k iters, topology frozen, seed 0, wandb online) + run_eval. Tool: `tools/gems_train/qem_prune.py` (new, reuses gems_pipeline stages; tag b3). Scenes: garden, toy_parking, courtyard @ B50. GPUs 3/5, supervised jobs.
- **PREDICTION (serves C1; direction + magnitude):** B3 lands BELOW B5 at matched budget on all 3 scenes (geometric decimation ignores render evidence AND moves vertex positions, destroying converged appearance that features-only FT can only partially repaint) — expected margin ≥ the D3 floor (B5−B3 ≥ +0.10 dB with paired CI excl. 0) on ≥2/3 scenes; the measured margin becomes the paper's B3 column. Refutation (B3 ≥ B5 anywhere with CI excl. 0) would BOUND C1's "evidence-based selection matters" component at B50. Secondary expectation: B3 ≥ B2 (random prune+FT) is NOT asserted — QEM preserves geometry but scrambles appearance carriers; either order is informative and will be reported.
- **H1 disposition (context row):** locate v106 historical artifacts (docs+outputs search); if found, record numbers as context-only JSON (never CI-compared — different mechanism/protocol era); else INFEASIBLE note with search evidence.
- **R1 disposition:** feasibility check ONLY (no training, no heavy installs): 3DGS availability in env/repo, setup cost estimate; if >1 day → INFEASIBLE-note draft per MATRIX §5 (needs human approval) with cross-representation positioning fallback.
- **AMENDMENT 1 (pre-run, before any B3 FT/eval number existed): decimation backend open3d → trimesh's QEM engine (fast_simplification 0.1.13), per the task-spec fallback order, on functional evidence:** smoke run on toy showed open3d's manifold-edge-collapse QEM stalls at 5.5% removal (6,590,546→6,231,952 faces vs target 3,295,279) — these checkpoints are non-manifold triangle soup (toy edge census: 44% of edges have ≥3 incident faces, up to 8; only 46% manifold-interior). fast_simplification (= the engine inside trimesh.Trimesh.simplify_quadric_decimation; sp4cerat quadric-edge-collapse, soup-tolerant, deterministic) at library-default agg=7 reaches the target in iterative rounds (a single round hits its internal iteration cap +21% above target on toy; rounds re-seed the collapse queue). No tuning: aggression stays at the library default, one rule for all scenes. Prediction unchanged.
- **AMENDMENT 2 (GPU allocation):** GOAL #014 claimed GPU 5 (coordinator directive, recorded in its entry) — all #013 B3 jobs run on GPU 3 only (jobs b3_gpu3a=garden, b3_gpu3b=toy→courtyard chained).

### GOAL #014 — B6R-on-SS3DM (T2 generalization of the E2R courtyard positive) + E11-QUAL grids [Matrix: E1-PARETO S-GEO B6R rows; E11-QUAL] — 2026-07-03 — IN PROGRESS
- **PART A PRE-REGISTRATION (frozen NOW, before any run):** mechanism = the EXACT #R-04 E2R-v1 recipe, no new knobs: FT from each town's B5@40000 (`models/ss3dm_town0{1,2,3}_B50_importance_ft_s2`), 10k iters → 50000, channels {features (default lr), opacity (`--enable_learnable_opacity`: opacity_lr 0.05, λ_o 1e-4, o_min 0.01 — frozen defaults from #R-00/#R-04)}, positions frozen (`--lr_triangles_points_init 0.0`), `--allow_fade_prune` (τ 0.05, T=3 @500), topology otherwise frozen; tag b6r; town06 EXCLUDED (its B50 B5 already fails the PSNR floor per #R-05, and g4/d1/d2 are INFEASIBLE per #R-08 — the B6R bar could not be evaluated there).
- **PREDICTION:** the courtyard-like E2R profile transfers to the real-street over-parameterized towns (they resemble courtyard, not toy): **g3 floater fraction improves ≥30% AND PSNR guard (mean ΔPSNR vs B5 ≥ −0.10) held on ≥2/3 towns.** ALSO tracked: d1 false-free predicted to worsen slightly (the R-02/R-08 coverage lesson) — recorded honestly as a BOUND on the B6R claim, not a pass/fail arm. Report per town: ΔPSNR paired CI vs B5 (baseline rows `eval/ss3dm_town0X_B50_geo_v1`), g1/g3/g4/d1/d2 changes, removed-triangle counts. Verdict tool: `tools/gems/e2r_verdict.py` UNCHANGED (single CI mouth via paired_bootstrap). Evals: `eval/b6r_ss3dm_town0X_B50_v1` (run_eval.py, single mouth). Jobs: supervised only (run_supervised.sh; b6r_gpu5 = town01→town03 on GPU 5; b6r_town02 = waits for the #012 e6abl_gpu1 chain to reach a terminal state, then GPU 1), IN-PROCESS jobs_wait.sh, WANDB_MODE=online (group gems_b6r), storage preflight in-chain, no git commits. NOTE for #013: GPU 5 is claimed by this goal per coordinator directive; #013's "GPUs 3/5" line predates this — coordinate via jobs dir.
- **PART B PLAN (rules frozen before generation):** new script `tools/gems/report/qual_grids.py` → `/data/peilincai/gems_stage1/RESULTS/figures/qual/<scene>_qual_grid.png` + `manifest.json` recording EVERY view-selection rule invocation. Scenes: S-REND garden/kitchen/flowers; S-GEO ss3dm_town01/ss3dm_town06; + toy_parking, courtyard. Grid rows = {clean, B50 B5, B25 B5} (+B6R where it exists: toy/courtyard = banked e2r_*_B50_v1; town01 = Part A output); cols per view-block = {RGB render, median-depth map (turbo, vmax = P99 of the CLEAN row's valid depth at that view, shared down the column), floater overlay (each row tinted by ITS OWN banked g3_floaters.npz ids — no recomputation)}. §5 crop rule per grid: (a) best = argmax banked per-view PSNR of the scene's B50 B5 row; (b) median = script-chosen `image_names[argsort(psnr)[(n−1)//2]]` from the SAME banked array (printed by the script); (c) failure = the E9-taxonomy view where a case names one (flowers _DSC9144 c01; town06 front_00000088 c02; toy 00035 c03; garden DSC08052 c12; kitchen DSCF0768 c13; courtyard DSC_0302 c08), else argmin banked per-view PSNR (town01 — no E9 case). Crop windows: computed ONCE from the B50 B5 row's |render−GT| error map at the selected view (best view → argmin-error half-frame window; failure view → argmax-error half-frame window; median view → full frame), then applied IDENTICALLY to every model row (paired comparability). Renders regenerated on GPU via tools/gems/eval_context at the banked checkpoints recorded in each row's metrics.json; supervised job.
- Baseline rows for grids (banked, single-mouth): garden {clean30k_v2, B50/B25_importance_ft_e1v2}, kitchen/flowers {clean30k_v1, B50/B25_importance_ft_s2}, town01/town06 {clean30k_geo_v1, B50_geo_v1, B25_importance_ft_s2}, toy {clean30k_v1, B50/B25_importance_ft_e1v2, e2r_toy_parking_B50_v1}, courtyard {clean30k_v1, B50/B25_importance_ft_e1v2, e2r_courtyard_B50_v1}.
- **PART A RESULT (2026-07-03, all 3 chains train+eval EXITED:0, verified via exit files + on-disk rows): PREDICTION NOT MET — B6R-on-SS3DM is DONE-FAIL as pre-registered (0/3 towns clear the joint bar; the g3 FRACTION arm fails everywhere).** Per-town (B6R − B5, paired bootstrap; verdicts `analysis/b6r_ss3dm/e2r_verdict_ss3dm_town0{1,2,3}.json`):
  - town01: ΔPSNR −0.027 CI[−0.095,+0.030] guard HELD; ΔLPIPS −0.0012 CI excl. 0 (better); g1 0.6412→0.6397 (−0.23%, CI excl. 0); g3 comps 10,395→7,109 (**−31.6%**) / frac 0.01225→0.01049 (**−14.3% < 30% bar**); d1-ff 0.8318→0.8387 (**+0.82% worse, CI excl. 0**) / fo 0.0608→0.0580 (better); g4 chamfer 1.648→1.651, F 0.0446→0.0451; d2 agree 0.685→0.685 unsafe 0.297→0.297; removed 246,528 (4.09%); FPS 70.1→70.7.
  - town02: ΔPSNR +0.032 CI[−0.021,+0.077] guard HELD; ΔLPIPS −0.0030 CI excl. 0; g1 −0.62% CI excl. 0; g3 comps 7,173→4,904 (**−31.6%**) / frac −22.7%; d1-ff +0.62% worse CI excl. 0 / fo better; g4 2.684→2.696, F 0.0310→0.0314; d2 agree 0.690→0.685 unsafe 0.301→0.306 (1 traj); removed 248,600 (3.19%); FPS 64.2→65.0.
  - town03: ΔPSNR +0.019 CI[−0.005,+0.042] guard HELD; ΔLPIPS −0.0008 CI excl. 0; g1 −0.20% CI excl. 0; g3 comps 8,026→5,335 (**−33.5%**) / frac −18.6%; d1-ff +1.13% worse CI excl. 0 / fo better; g4 2.009→2.012, F 0.0515→0.0529; d2 identical agree/unsafe; removed 248,988 (3.29%); FPS 65.0→67.0.
  - **Interpretation (one honest paragraph):** the guard held 3/3 (CI-lo > −0.10 everywhere — stronger than courtyard's wide CI) and the E2R *direction* transferred (LPIPS, g1, g3-components all better with CIs excl. 0 on every town; component cleanup is eerily uniform at −32±2%), but the courtyard *magnitude* did NOT: floater FRACTION improves only −14..−23% vs the pre-registered ≥30%, i.e. fading removes many small components but the towns' floater triangle mass sits in larger supported structures that resist the opacity decay. d1 false-free worsened +0.6..+1.1% (CI excl. 0) on all 3 towns exactly as the R-02/R-08 coverage lesson predicted — this BOUNDS B6R on the safety-critical axis. g4/d2 preservation-flat. No toy-style FPS collapse (SS3DM stacks are shallower). VERDICT: **B6R remains a bounded, courtyard-scoped positive; NOT promotable toward C2′ from this cell; no further B6R variants sanctioned here (E2R budget stays 1/2 used at #R-04).** wandb group gems_b6r (3 runs, online). Ops notes: town02 re-pointed from GPU 1 (ablation chain held it) to freed GPU 5 — old waiter killed BY PIDFILE, relaunched as job b6r_town02_gpu5; background/monitor notifications were repeatedly FABRICATED this session (claimed files/exit states that did not exist, incl. a future timestamp) — every completion was therefore re-verified synchronously on disk before any dependent action; only exit files + metrics.json on disk were trusted.
- **PART B RESULT (2026-07-03): E11-QUAL grids DONE (7/7).** `RESULTS/figures/qual/{garden,kitchen,flowers,ss3dm_town01,ss3dm_town06,toy_parking,courtyard}_qual_grid.png` + `manifest.json` (frozen rules + all 21 view-selection invocations with banked-array paths, chosen views, PSNRs, crop windows, per-row checkpoints/floater-npz provenance). Deviation from plan (logged): output root = repo `RESULTS/` (convention set same-day by the evidence-pack agent's `tools/gems/report/collect.py`), not gems_stage1. Rule coincidences (honest, from frozen rules): garden/kitchen/toy median-PSNR view == the E9 failure view (blocks (b) and (c) show the same view, different crops). B6R rows included in toy/courtyard/town01 grids. Notable visible evidence: courtyard failure-view (DSC_0302) black near-camera occluder GONE in the B6R row (+2.7 dB at that view); toy failure crop shows the E9 case03/04/09 bottom-band pinholes in all compacted rows; town01 failure view (front_right_00000000, 9.4 dB trajectory start) is majority floater-flagged in every compacted row. Jobs qualgrid_{toy,srend,towns,rest} all EXITED:0; town grids re-run after a manifest-overwrite bug was found and fixed in the script (merge with existing manifest; PNGs deterministic, unchanged). MATRIX: E11-QUAL grids → DONE (videos remain T2 TODO); E1-PARETO S-GEO B6R note added. GOAL #014 CLOSED.
