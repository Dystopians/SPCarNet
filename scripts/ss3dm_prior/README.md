# SS3DM Prior Scripts

This directory is reserved for scripts whose primary ownership is the SS3DM prior line.

Separation rule:

- `car model` scripts live in `scripts/car_model/`
- shared Python modules may still be imported from the same repository
- script placement follows task ownership, not just implementation reuse

Current SS3DM preprocessing entrypoint:

- `pre_process_ss3dm.sh`
