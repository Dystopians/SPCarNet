# Final Go / No-Go

Current decision: `STRONG_WORKSHOP_OR_ARXIV_NEEDS_ONE_MORE_PUBLIC_POSITIVE`.

R53/R55 are strong parking results, and R58 adds a public-scene matched clean-to-compact replication on bonsai. The current package is stronger than a single-scene result, but R57 courtyard is a controlled negative and the ablation suite still has interface-only rows.

Upgrade to `GO_NEURIPS` only after:
1. R53-like clean-to-compact dominance replicates on at least 2/3 public scenes, or a validated selector predicts the R57 failure before recovery.
2. Ablation suite shows at least three core components are empirically necessary.
3. Giant-hole repair has synthetic plus at least one real/realistic non-hallucinated example.
